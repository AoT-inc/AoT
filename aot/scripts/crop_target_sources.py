# coding=utf-8
"""작물종별 **단계 목표값과 그 출처** — 템플릿 카탈로그의 재료.

정본: docs/design/program-layer.md

`seed_programs.catalog()` 가 이 표를 읽어 템플릿의 목표를 채운다. 값과 출처를
**같은 자리**에 두는 이유는, 나중에 이 숫자를 고칠 사람이 "어느 자료의 어느 표에서
왔는가" 를 코드에서 바로 볼 수 있어야 하기 때문이다. 출처가 없으면 고칠 근거도 없고,
그때는 지어낸 값과 구분되지 않는다.

## 지어내지 않는다

**자료에 없는 항목은 비운다.** 채워진 숫자는 빈 칸보다 강한 주장이라 사람은 그것을
"조사된 추천값" 으로 읽는다. 실제로 국내 재배 지침은 온도를 잘 다루고 습도·CO₂ 는
드물게, DLI·VPD 는 거의 다루지 않는다 — 그래서 그 칸들이 비는 것이 정상이다.

## 범위를 단일값으로 옮기는 규칙

지침은 대개 범위로 적혀 있다(`25~27℃`). 프로그램의 목표는 한 값이므로 다음 규칙을
**한 벌만** 쓴다. 규칙을 값마다 달리 하면 나중에 비교할 수 없다.

1. **범위 → 중앙값**, 소수 한 자리에서 반올림 (`25~27` → `26`).
2. **"전후"·"내외" → 그 값 그대로** (`25℃ 전후` → `25`).
3. **하한만 있는 값("이상")은 목표로 쓰지 않는다** — 그것은 한계이지 목표가 아니다
   (이 저장소가 목표를 한계 필드에 넣지 않기로 한 것과 같은 판단).
4. 오전·오후가 나뉘어 있으면 **두 구간을 합친 전체 범위의 중앙값**을 쓴다.
   시간대별 목표는 Method 곡선의 일이지 단계 값의 일이 아니다.

## 단계 전체에 같은 값이면 `defaults` 로 올린다

`defaults` 는 프로그램의 기본 목표값(`target_defs[].default`)이 되고, `stages` 는
그 단계에서만 다른 값이다. 자료가 "생육기 65~80%" 처럼 작기 내내 같은 값을 말하면
단계마다 되풀이하지 않는다 — 되풀이하면 고칠 때 한 곳만 고쳐진다.

## 쓰지 않기로 한 자료

- **RDA 스마트팜 생산성 향상 모델**(`smartfarm.rda.go.kr`, 완숙토마토·딸기·파프리카·
  참외·오이): 우량농가 데이터에서 뽑은 실측 최적환경이라 자료의 질은 높지만,
  단계 구분이 **달력 기준**(8월·9~10월·…·3~6월)이고 설정값이 누적일사량 4구간 ×
  외기기온 3구간으로 갈린다. 우리 단계 어휘(육묘기·개화기·착과기)로 옮기려면 없는
  대응을 지어내야 한다.
- 같은 자료의 **CO₂ 는 "주간 잔존 CO₂ 농도"** 다 — 환기 결과로 남은 **측정값**이지
  시비 목표가 아니다(383~470 ppm 은 대기 농도에 가깝다). 목표로 옮기면 뜻이 뒤집힌다.
- **스마트팜코리아 setpoint API**: 이쪽이 사람이 옮긴 표보다 낫지만
  (`ai/context/ext/smartfarm_client.py`) `RDA_API_KEY` 가 없으면 캐시가 비어 있다.
  키가 등록되면 그쪽 값으로 갱신하는 것이 옳다.
"""

# 출처 문서. 값 옆에 `src` 키로 참조한다 — 같은 작물이라도 항목마다 출처가 다를 수
# 있어 문서를 한 번만 적고 가리킨다.
SOURCES = {
    'nafqs_cherry_tomato': {
        'title': '국립농산물품질관리원 「방울토마토(토경) 표준재배지침」',
        'note': ('생육단계별 온도·습도·CO₂ 를 단계 이름으로 제시한다. 원문 PDF 를 '
                 '직접 받지 못해 전재본으로 확인했다(2026-08-20). 값을 고칠 때는 '
                 '원문을 다시 확인할 것.'),
        'url': 'https://m.cafe.daum.net/hygrotec/ExXe/19',
    },
    'nongsaro_paprika': {
        'title': '농사로(농촌진흥청) 파프리카 재배 정보',
        'note': '모 기르기·열매달림 단계의 낮/밤 온도와 탄산가스 농도를 준다.',
        'url': ('https://www.nongsaro.go.kr/portal/ps/psb/psbl/workScheduleDtl.ps'
                '?menuId=PS00087&cntntsNo=30649'),
    },
    'nongsaro_cucumber': {
        'title': '농사로(농촌진흥청) 오이 재배 정보 · 제주농업기술원 과채류(오이)',
        'note': '작물 단위 생육적온만 낮/밤으로 준다. 단계별 세분은 없다.',
        'url': ('https://www.nongsaro.go.kr/portal/ps/psz/psza/contentSub.ps'
                '?menuId=PS03172&sSeCode=335001&cntntsNo=101620'),
    },
    'nongsaro_strawberry': {
        'title': '농사로(농촌진흥청) 딸기(촉성재배) 재배 정보',
        'note': ('주간 15~25℃(광합성 90% 이상)를 제시한다. 야간은 "봄가을 10℃ '
                 '이상·겨울 5℃ 이상" 처럼 **하한**이라 목표로 쓰지 않고, 별도 '
                 '자료의 "밤 10℃ 내외" 를 야간 목표로 삼았다.'),
        'url': ('https://www.nongsaro.go.kr/portal/ps/psb/psbl/workScheduleDtl.ps'
                '?menuId=PS00087&cntntsNo=30610'),
    },
    'chungbuk_lettuce': {
        'title': '충청북도농업기술원 상추 재배기술',
        'note': ('육묘 주간온도만 준다. 검색 요약에 돌던 "육묘 낮 22~23 / 정식 밤 '
                 '14~15" 는 이 원문에 **없어** 쓰지 않았다 — 출처를 확인하지 못한 '
                 '값은 넣지 않는다.'),
        'url': 'https://ares.chungbuk.go.kr/home/sub.php?menukey=1157',
    },
    'nongsaro_spinach': {
        'title': '농사로(농촌진흥청) 시금치 재배 정보',
        'note': ('"발아 및 생육적온 15~20℃" 로 **낮·밤을 나누지 않은** 값만 준다. '
                 '우리 어휘에는 낮/밤 항목뿐이라 그 값을 어느 쪽에도 넣을 수 없다 '
                 '— 넣으면 자료에 없는 구분을 지어내는 것이 된다. 그래서 목표는 '
                 '비우고 지침만 남긴다.'),
        'url': ('https://www.nongsaro.go.kr/portal/ps/psb/psbl/workScheduleDtl.ps'
                '?menuId=PS00087&cntntsNo=30628'),
    },
    'nongsaro_tomato': {
        'title': '농사로(농촌진흥청) 토마토 재배 정보',
        'note': '작물 단위 적온만 제시하고 단계별 세분은 없다.',
        'url': ('https://www.nongsaro.go.kr/portal/ps/psz/psza/contentSub.ps'
                '?menuId=PS03172&sSeCode=335001&cntntsNo=101611'),
    },
}


# ── 작물종별 목표 ────────────────────────────────────────────────────
#
# `defaults` = 작기 내내 같은 값(프로그램 기본값)
# `stages`   = 그 단계에서만 다른 값
# `src`      = 그 묶음의 출처 키
#
# ⚠ 단계 키는 `growth_stage_resolver.STAGE_DURATION_MAP` 의 것과 같아야 한다.
#   여기 없는 단계는 기본값을 따르고, 기본값도 없으면 빈 칸이다.
SPECIES_TARGETS = {
    # ── 방울토마토 ──────────────────────────────────────────────────
    # 표준재배지침이 단계 이름으로 값을 주는 드문 자료라 가장 촘촘하다.
    'cherry_tomato': {
        'src': 'nafqs_cherry_tomato',
        'defaults': {
            # "정식후(변온) … 습도 65~80%" — 생육기 전반에 걸린 값이라 기본값으로.
            'rh': 72.5,
        },
        'stages': {
            # 원문의 "정상육묘(주간 25℃ 전후/야간 15~16℃)" 값은 여기 두지 않는다
            # — 육묘는 이 구획이 아닌 다른 곳에서 하므로 'seedling' 이 이 작물의
            # 단계 목록에 없다(growth_stage_resolver.STAGE_DURATION_MAP 참조).
            # "정식후(변온) : 오전 25~28℃ / 오후 23~25℃, 야간 13~15℃"
            # 규칙 4 — 오전·오후를 합친 23~28 의 중앙값.
            'transplant': {'temp_day': 25.5, 'temp_night': 14.0},
            # "개화기전 : 낮 25~27℃ / 밤 17℃"
            'vegetative': {'temp_day': 26.0, 'temp_night': 17.0},
            # "개화·착과기 적온 20~25℃", "CO₂ 800~1,200ppm(맑은날)"
            # 흐린날 500~800 은 **날씨에 따라 달라지는 값**이라 단계 목표로 옮기지
            # 않는다 — 그것까지 담으려면 목표가 아니라 조건부 규칙이 필요하다.
            'flowering':  {'temp_day': 22.5, 'co2': 1000.0},
            'fruit_set':  {'temp_day': 22.5, 'co2': 1000.0},
            # 결실기·수확기의 구체 수치는 지침에 없다 → 비운다.
        },
    },

    # ── 토마토 ──────────────────────────────────────────────────────
    # 농사로는 작물 단위 적온만 준다. 단계별로 나눌 근거가 없으므로 **기본값
    # 하나**로 두고 단계는 비운다 — 같은 값을 단계마다 되풀이하면 조사해서 단계별로
    # 다르게 정한 것처럼 보인다.
    'tomato': {
        'src': 'nongsaro_tomato',
        'defaults': {
            # "잘 자라는 온도 : 낮 25~30℃, 밤 18~20℃"
            'temp_day': 27.5,
            'temp_night': 19.0,
        },
        'stages': {},
    },

    # ── 파프리카 ────────────────────────────────────────────────────
    'paprika': {
        'src': 'nongsaro_paprika',
        'defaults': {},
        'stages': {
            # 원문의 "모 기르기(낮 22~25℃/밤 18~20℃/배지 22℃ 내외)" 값은 여기
            # 두지 않는다 — 육묘는 이 구획이 아닌 다른 곳에서 하므로 'seedling'
            # 이 이 작물의 단계 목록에 없다(growth_stage_resolver 참조).
            # "열매달림 : 낮 25℃ / 밤 17℃, 탄산가스 600ppm 유지"
            'fruit_set': {'temp_day': 25.0, 'temp_night': 17.0, 'co2': 600.0},
        },
    },

    # ── 오이 ────────────────────────────────────────────────────────
    # 낮/밤이 나뉜 작물 단위 적온만 있다. 단계별 세분은 자료에 없다.
    'cucumber': {
        'src': 'nongsaro_cucumber',
        'defaults': {
            # "생육 적온 : 주간 22~28℃, 야간 15~18℃"
            'temp_day': 25.0,
            'temp_night': 16.5,
        },
        'stages': {},
    },

    # ── 딸기 ────────────────────────────────────────────────────────
    'strawberry': {
        'src': 'nongsaro_strawberry',
        'defaults': {
            # "주간 15~25℃(광합성 90% 이상)" — 중앙값. 별도 자료의 "낮 20℃ 전후"
            # 와도 같은 값이라 서로 뒷받침한다.
            'temp_day': 20.0,
            # 농사로의 야간 값("봄가을 10℃ 이상")은 **하한**이라 규칙 3 으로 쓰지
            # 않고, "밤 10℃ 내외"(규칙 2)를 쓴다.
            'temp_night': 10.0,
        },
        'stages': {},
    },

    # ── 상추 ────────────────────────────────────────────────────────
    # 원문이 주는 것은 육묘 주간온도뿐이다. 그 이상은 넣지 않는다.
    'lettuce': {
        'src': 'chungbuk_lettuce',
        # 원문의 "육묘 평균 15~20℃(주간온도)" 값은 여기 두지 않는다 — 육묘는
        # 이 구획이 아닌 다른 곳에서 하므로 'seedling' 이 이 작물의 단계
        # 목록에 없다(growth_stage_resolver 참조). 이 원문은 육묘 값 하나뿐이라
        # 지금은 목표가 전부 빈다.
        'defaults': {},
        'stages': {},
    },

    # ── 시금치 ──────────────────────────────────────────────────────
    # 목표는 **비운다** — 자료가 낮·밤을 나누지 않아 어느 항목에도 넣을 수 없다.
    # 지침만 남긴다(`SPECIES_GUIDANCE`).
    'spinach': {
        'src': 'nongsaro_spinach',
        'defaults': {},
        'stages': {},
    },
}


# ⚠ **단계 지침은 여기 두지 않는다.** `seed_programs._STAGE_GUIDANCE` 가 정본이다 —
# 본문과 출처를 한 짝으로 요구하고, 출처가 비면 싣지 않는다. 두 곳에 두면 어느
# 쪽이 화면에 나가는지 알 수 없게 된다(이 저장소가 반복해서 겪은 실패).
