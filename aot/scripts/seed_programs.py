#!/usr/bin/env python3
"""재배 프로그램 **템플릿 카탈로그** — 사람이 고를 때 꺼내 쓰는 예시.

⚠ **더 이상 내장 프로그램을 미리 깔지 않는다**(2026-08-19 방향 전환). 쓰지도 않는
작물 7종이 목록에 먼저 들어가 있으면, 사용자는 자기 것을 찾기 전에 남의 것을
지나쳐야 한다. AoT 는 농장 전용이 아니라 공원·체육시설·교통시설에도 쓰이므로
"채소 7종" 이 기본값인 것은 특히 좁다.

카탈로그는 **코드 상수**로만 존재하고, 화면의 "템플릿에서 시작" 에서 고를 때
비로소 사용자 프로그램(`source='user'`)으로 만들어진다.

정본: docs/design/program-layer.md

## 두 층 — 카테고리가 먼저, 작물종은 그 아래(2026-08-19)

세부 작물 단위로만 나열하면 목록이 늘어날수록 고르는 부담이 커진다. 그래서
**카테고리(넓은 범주, 예: 과채류)를 먼저 보여주고, 작물종은 "더 구체적으로"
고르는 선택지로 둔다.** 카테고리로 만든 프로그램은 사람이 화면에서 실제 대상에
맞게 고치는 것을 전제로 한다 — `_CATEGORY_MAP` 의 설명을 보라.

**카테고리는 소속 작물이 있어야만 만든다.** 스켈레톤 일수는 소속 작물들의
`STAGE_DURATION_MAP` 값을 통계로 요약한 것이지 새로 지어낸 값이 아니다
(`_category_stages()` 참조).

### 얼마나 만들 수 있나 — 점검 결과(2026-08-19)

카테고리 이름 자체는 `ext_translation_table.CROP_NAME_MAP` 이 이미 5갈래로 나눠
두고 있다(과채류·엽채류·근채류·허브·화훼류 — AI 번역표 용도로 먼저 생겼다).
그런데 **단계·기간 실측 자료(`STAGE_DURATION_MAP`)가 있는 작물은 7종뿐이고,
그 7종이 5갈래 중 단 2갈래(과채류·엽채류)에만 들어 있다.** 나머지 세 갈래는
이름은 있어도 소속 작물이 0종이라 지금은 만들 수 없다:

| 갈래 | `CROP_NAME_MAP` 상 종수 | `STAGE_DURATION_MAP` 상 종수 | 카테고리 |
|------|------------------------|------------------------------|----------|
| 과채류 | 12 (토마토·오이·딸기 등) | 5 | ✅ 지금 만들어진다 |
| 엽채류 | 9 (상추·배추·케일 등) | 2 | ✅ 지금 만들어진다 |
| 근채류 | 4 (무·당근·감자·고구마) | 0 | ⏳ 대표 작물 실측 자료 없음 |
| 허브류 | 6 (바질·파슬리·민트 등) | 0 | ⏳ 동일 |
| 화훼류 | 6 (장미·국화·난 등) | 0 | ⏳ 동일 |

`GeoProgram.kind`(대상 종류) 축으로 한 단계 더 올려 봐도 같다 — `vegetation` 외의
`livestock`·`facility`·`other` 는 이 저장소 어디에도 단계·기간 표가 없다(가축·
시설물 전용 소스가 아직 없다). 그래서 **지금 실제로 만들 수 있는 카테고리는
정확히 2개**이고, 이 스크립트가 만드는 것도 그 2개뿐이다. 나머지는 이름만
예약해 둔다(`_CATEGORY_MAP` 에 있지만 소속이 없어 `catalog()` 가 걸러낸다) —
대표 작물이 `STAGE_DURATION_MAP` 에 추가되면 이 파일은 손댈 것 없이 그 카테고리가
자동으로 생긴다.

### 왜 지금 만드나 — 뒤로 미루는 비용

이 계층은 **DB 에 아무것도 저장하지 않는다** — `catalog()` 는 매번 다시 계산하는
순수 함수라, 나중에 추가해도 마이그레이션이나 기존 데이터 백필이 필요 없다.
그런데도 지금 만드는 이유는 두 가지다:

1. **이름은 지금 정하는 편이 싸다.** 카테고리 이름을 화면에 노출한 뒤 바꾸면
   사용자가 다시 배워야 한다. `CROP_NAME_MAP` 이 이미 쓰는 이름을 그대로 가져다
   쓰면, 나중에 세 번째·네 번째 카테고리가 생겨도 이름을 새로 짓는 일이 없다.
2. **목록은 작물종이 늘수록 나빠진다.** 지금 7종에서 나중에 20~30종으로 늘면
   평면 목록은 그때부터 부담이 되고, 그 시점에 카테고리를 소급 적용하는 것보다
   처음부터 두 층으로 짜 두는 쪽이 항목 하나 늘 때마다의 비용이 낮다.

## 무엇을 옮기는가

- 단계와 기간: `aot/ai/context/growth_stage_resolver.py` 의 `STAGE_DURATION_MAP`
  (RDA 시설재배 지침 기반, 지금은 **AI 만** 읽는다)
- 광합성 파라미터·권장 목표: `aot/scripts/seed_crop_presets.py` 의 `_CROP_PRESETS`
  (그때는 **제어만** 읽었다)

둘은 같은 작물을 말하면서 서로를 모른다. 여기서 한 프로그램으로 합친다.
합친 뒤로는 **프로그램이 정본**이다 — 제어도 AI 도 프로그램을 읽고, 위 두 곳은
템플릿을 만들 때의 재료로만 남았다(`docs/design/coordinator-plot-targets.md`).

## 누적일 → 단계 길이

`STAGE_DURATION_MAP` 은 **누적 일수**(그 단계가 끝나는 날)로 적혀 있다. 프로그램의
`days` 는 **그 단계의 길이**다 — 누적으로 두면 중간 단계를 늘릴 때 뒤 단계를 전부
손봐야 한다. 여기서 차분으로 변환한다.

마지막 단계의 `999` 는 "끝까지" 라는 뜻이라 길이로 옮기지 않는다(`days=None`).
수확기는 사람이 끝낼 때까지다.

## 목표(`targets`)는 채우지 않는다

`targets` 는 채우지 않는다. 잠깐 광합성 프리셋의 **작물 단위** 값을 모든 단계에
복사했는데, 그것은 **단계별 값이 아니다** — 육묘기와 착과기의 목표가 같을 리 없고,
같은 값을 여러 칸에 채워 두면 사람은 그것을 "조사된 추천값" 으로 읽는다. 채워진
숫자는 빈 칸보다 강한 주장이다(2026-08-19 되돌림). 단계별 목표는 **실제 조사로**
채운다(작물별 재배 지침·시험 자료). 그 전까지는 비워 두고, 사람이 자기 재배
방식대로 적는다. 카테고리 템플릿은 작물종보다도 더 넓은 범위를 뭉뚱그리므로,
목표를 지어낼 근거가 더 약하다 — 마찬가지로 비운다.

## 단계 지침(`guidance`) — 시스템이 못 하는 관찰·행동을 요약해 담는다

`_STAGE_GUIDANCE` 는 **시스템이 대신해 줄 수 없는 관찰과 행동**을 담는다 —
눈으로 봐야 아는 것, 손으로 해야 하는 것. 수치 목표는 `SPECIES_TARGETS` 가 갖고,
환경을 맞추는 것만으로 좋아지지 않는 나머지 절반이 이쪽이다. 초보 사용자와 AI 가
함께 보는 자리라 비워 두면 그 절반이 통째로 빠진다.

**원문을 전재하지 않고 요약한다.** RDA 표준재배매뉴얼·농사로·도 농업기술원 자료는
저작물이라 그 산문을 공개 저장소의 코드 상수로 옮겨 적을 것이 아니다. 핵심만 우리
말로 줄여 적고, 더 알아야 할 것은 사용자가 원문을 찾거나 AI 에게 묻는다 — 시스템이
모든 것을 담을 수는 없다. (자료를 통째로 가져오는 통로는 `ext/nongsaro_client.py`
가 따로 갖고 있다 — 오픈API 라 출처가 붙고 원본 갱신을 따라간다.)

규율은 하나다: **본문과 출처를 함께 적는다.** 출처가 비면 `_guidance_for()` 가 그
지침을 싣지 않고, 회귀 검사가 실패한다. 요약을 읽고 원문으로 갈 수 있어야 한다.

## `--purge-builtin`

예전 버전이 깔아 둔 내장 프로그램을 걷어낸다. **참조 중인 것은 남긴다** — 지우면
그 작기가 "무엇을 목표로 길렀나" 의 근거를 잃는다.

사용:
    python3 -m aot.scripts.seed_programs --list     # 카탈로그 보기
    python3 -m aot.scripts.seed_programs --purge-builtin
        # 예전에 깔린 내장 프로그램을 걷어낸다(참조 중인 것은 남긴다)

종료 코드 0 = 정상, 2 = 실패.
"""
import argparse
import json
import statistics
import sys

# 단계 키 → 표시 이름(한국어). 화면 번역은 msgid 로 따로 하고, 여기에는 시드가
# 스스로 읽히도록 기본 이름을 넣는다. **딕셔너리 삽입 순서 = 생육 진행 순서**다 —
# 카테고리 스켈레톤을 정렬할 때 이 순서를 그대로 쓴다(_STAGE_ORDER).
_STAGE_NAMES = {
    'seedling':        '육묘기',
    'germination':     '발아기',
    'sowing':          '파종기',
    'transplant':   '정식기',
    'vegetative':      '영양생장기',
    'vegetative_early': '초기생장',
    'vegetative_late': '후기생장',
    'flower_initiation': '화아분화기',
    'flowering':       '개화기',
    'fruit_set':       '착과기',
    'fruit_development': '비대기',
    'fruiting':        '결실기',
    'runner':          '런너기',
    'pre_harvest':     '수확전기',
    'harvest':         '수확기',
    'post_harvest':    '수확후기',
}
_STAGE_ORDER = {key: i for i, key in enumerate(_STAGE_NAMES)}

_CROP_NAMES = {
    'tomato':        '토마토',
    'cherry_tomato': '방울토마토',
    'paprika':       '파프리카',
    'cucumber':      '오이',
    'strawberry':    '딸기',
    'lettuce':       '상추',
    'spinach':       '시금치',
}

# 광합성 프리셋의 작물 키가 다른 경우의 대응(파프리카 = pepper 프리셋).
_PRESET_ALIAS = {'paprika': 'pepper', 'cherry_tomato': 'tomato'}

# 카테고리 — 이름은 `ext_translation_table.CROP_NAME_MAP` 이 이미 쓰는 5갈래를
# 그대로 가져온다(새 어휘를 짓지 않는다). **소속이 있어야 만든다** — 근채류·
# 허브류·화훼류는 지금 `STAGE_DURATION_MAP` 에 대표 작물이 하나도 없어 이름만
# 예약해 둔다(catalog() 가 실행 시점에 소속을 걸러낸다 — 여기 있어도 소속이
# 0이면 카탈로그에 나타나지 않는다). 대표 작물이 `STAGE_DURATION_MAP` 에
# 추가되면 이 표에 소속만 적어 넣으면 된다(위 "얼마나 만들 수 있나" 참조).
_CATEGORY_MAP = {
    'fruiting_vegetable': {
        'name': '과채류',
        'members': ['tomato', 'cherry_tomato', 'paprika', 'cucumber', 'strawberry'],
    },
    'leafy_vegetable': {
        'name': '엽채류',
        'members': ['lettuce', 'spinach'],
    },
    'root_vegetable': {'name': '근채류', 'members': []},   # 무·당근·감자·고구마 — 대기
    'herb':           {'name': '허브류', 'members': []},   # 바질·파슬리·민트 등 — 대기
    'ornamental':     {'name': '화훼류', 'members': []},   # 장미·국화·난 등 — 대기
}

# 작물종 → 소속 카테고리 키(역방향 조회, catalog() 가 작물종 항목에 붙인다).
_SPECIES_CATEGORY = {
    m: cat_key for cat_key, cat in _CATEGORY_MAP.items() for m in cat['members']
}


# 단계 지침(`guidance`) — 관찰·행동을 요약해 담는다(2026-08-20).
#
#     (작물키, 단계키): ('지침 본문', '출처 한 줄')
#
# ## 왜 비어 있나 — 지어낼 수 없어서가 아니라 지어내면 안 되어서
#
# 지침은 자유 텍스트라 범위 검사가 없다. 단계 일수는 `STAGE_DURATION_MAP` 과
# 대조라도 되지만, 지침은 **무엇과도 대조되지 않는다** — 이 저장소에서 가장
# 지어내기 쉬운 필드다. 그리고 채워진 글은 빈 칸보다 강한 주장이라(2026-08-19
# 목표값을 되돌린 것과 같은 이유), 사람은 템플릿에 적힌 문장을 "조사된 지침" 으로
# 읽고 그대로 따른다.
#
# 지금 이 저장소가 가진 작물 지식은 두 표뿐이고 **둘 다 지침이 아니다**:
#
#   - `STAGE_DURATION_MAP` — 단계 길이(일수)만 있다. 무엇을 하라는 말이 없다.
#   - `_CROP_PRESETS` — 광합성 파라미터·작물 단위 상수다. 단계별 값이 아니다.
#
# ## 재배 지침서를 여기에 옮겨 적지 않는 이유
#
# RDA 표준재배매뉴얼·농사로·도 농업기술원 자료는 **저작물**이다. 정확한지와 별개로,
# 그 산문을 공개 저장소의 코드 상수로 옮겨 적는 것은 라이선스 문제다(이 저장소는
# 이미 vendor 라이선스를 따로 정리해 두었다 — THIRD-PARTY-LICENSES.md).
#
# 그리고 그 내용을 가져오는 **올바른 통로가 이미 있다**:
# `aot/ai/context/ext/nongsaro_client.py` 가 농사로 오픈API 로 작물별 재배
# 가이드를 받아 `ext_nongsaro_guides` 에 캐시한다(운영자 자기 API 키로). 출처가
# 붙은 채로 오고, 원본이 갱신되면 따라 갱신된다 — 여기 베껴 둔 문장은 그러지
# 못한다.
#
# ## 그래도 채운다면 — 출처 없는 지침은 실리지 않는다
#
# 이 표에 항목을 더할 때는 **본문과 출처를 함께** 적는다. 출처가 비면
# `_guidance_for()` 가 그 지침을 아예 싣지 않는다(그리고 test_geo_plot.py 의
# 회귀 검사가 실패한다). 출처는 되짚을 수 있는 한 줄이어야 한다 — 발행처와 자료
# 이름, 가능하면 연도까지. "일반적으로 알려진 것" 은 출처가 아니다.
#
# 형식(자리표시자다 — 실재하는 자료를 흉내 내지 않는다):
#
#     ('<작물키>', '<단계키>'): (
#         '<그 단계에 무엇을 어떻게 하는가 — 원문의 말로>',
#         '<발행처>, <자료 이름>(<연도>) <쪽·항 번호>'),
#
# 카테고리 템플릿(과채류·엽채류)에는 **지침을 붙이지 않는다.** 소속 5종을
# 뭉뚱그린 자리라 어느 작물의 지침도 그 범위의 지침이 아니다 — 일수는 중앙값으로
# 요약이라도 되지만 산문은 요약되지 않는다.
# `(작물키, 단계키)` → `(지침, 출처)`. **한 짝이 아니면 실리지 않는다**
# (`_guidance_for`).
#
# ## 무엇을 적는가 — 시스템이 못 하는 일
#
# 온도·습도·CO₂ 같은 수치는 목표값(`SPECIES_TARGETS`)이 갖는다. 여기 적는 것은
# **센서와 제어가 대신해 줄 수 없는 관찰과 행동**이다: 눈으로 봐야 아는 것, 손으로
# 해야 하는 것, 때를 놓치면 되돌릴 수 없는 것. 환경을 맞추는 것만으로는 좋아지지
# 않는 부분이 재배의 절반이고, 그쪽을 초보 사용자와 AI 가 함께 볼 자리다.
#
# ## 원문을 옮겨 적지 않는다 — 요약한다
#
# RDA·농사로·도 농업기술원 자료는 저작물이다. 그 산문을 공개 저장소의 코드 상수로
# 전재하지 않고, **핵심만 우리 말로 줄여 적는다.** 더 알아야 할 것은 사용자가
# 원문을 찾거나 AI 에게 묻는 구조다 — 시스템이 모든 것을 담을 수는 없다.
# 출처를 함께 적는 이유가 그것이다: 요약을 읽고 원문으로 갈 수 있어야 한다.
_STAGE_GUIDANCE = {
    # ── 방울토마토 ────────────────────────────────────────────────
    # ⚠ 'seedling' 지침은 두지 않는다 — 육묘는 대개 이 구획이 아닌 다른 곳에서
    # 하므로(growth_stage_resolver.STAGE_DURATION_MAP 의 anchor 설명 참조),
    # 이 구획의 첫 단계는 정식이다.
    #
    # 챗봇 질문에 우리 단계 어휘를 그대로 넣어 답을 받았다 — 답이 "개화기/착과기"
    # 를 한 덩이로 줘서, 개화(꽃·인공수분) 이야기는 flowering 에, 착과 수 점검·
    # 순지르기·웃거름 이야기는 fruit_set 에 나눠 담았다.
    ('cherry_tomato', 'transplant'): (
        '모종의 본엽이 8~9매, 제1화방 꽃이 핀 상태인지, 심는 깊이와 토양 상태 '
        '(습도·통기성), 초세(너무 크거나 작지 않은지)를 본다. 적정 재식밀도 '
        '(3.3㎡당 8~10주)로 정식하고, 뿌리 주변 토양을 다져 관수하며 버팀목을 '
        '세운다.',
        '작물별 시비처방 기준(개정증보판, 67쪽)·농업기술길잡이 106 토마토 '
        '(90쪽) — 농사로 AI 이삭이 농업백과 경유'),
    ('cherry_tomato', 'vegetative'): (
        '줄기 굵기, 생장점 색상과 전개엽수(건강 여부), 잎 색과 형태(병해·영양 '
        '결핍, 시들음·황화 등), 초세(지나친 도장·부족한 생육)를 본다. 특히 '
        '시들음병·줄기썩음병·잎마름역병 징후(줄기 갈변, 잎 가장자리 마름, 포기 '
        '전체 시들음)를 놓치지 않는다. 곁순(측지)을 제거하고 버팀목을 추가로 '
        '고정하며, 필요하면 첫 웃거름을 준다.',
        '작물별 시비처방 기준(개정증보판, 67쪽)·농업기술길잡이 106 토마토 '
        '(90쪽)·현장기술지원 사례_친환경 방울토마토 시들음 피해 현장 기술지원 '
        '(1쪽) — 농사로 AI 이삭이 농업백과 경유'),
    ('cherry_tomato', 'flowering'): (
        '꽃이 필 때 정상 개화 여부와 화분의 상태를 본다. 필요하면 진동기나 '
        '털봇으로 인공수분을 한다.',
        '작물별 시비처방 기준(개정증보판, 67쪽)·농업기술길잡이 106 토마토 '
        '(90쪽) — 농사로 AI 이삭이 농업백과 경유'),
    ('cherry_tomato', 'fruit_set'): (
        '화방마다 착과 수를 점검하고, 저온·고온·일조량 부족 등에 의한 생리장해 '
        '여부를 본다. 추가 곁순을 제거하고 순지르기(맨 위 생장점을 잘라줌)를 '
        '하며, 줄기 길이가 2cm 이상 자랐을 때 복합비료로 웃거름을 소량 준다.',
        '작물별 시비처방 기준(개정증보판, 67쪽)·농업기술길잡이 106 토마토 '
        '(90쪽) — 농사로 AI 이삭이 농업백과 경유'),
    ('cherry_tomato', 'fruiting'): (
        '과실 크기(비대 상태)와 착색 정도, 과피 균열(열과), 병징(시들음·줄기 '
        '썩음·잎마름 등)을 본다. 과다 착과 시 작은 열매를 솎아내고(적과), 병든 '
        '과실·잎을 제거하며 지주·유인 작업과 곁가지 정리를 이어간다.',
        '작물별 시비처방 기준(개정증보판, 67쪽)·농업기술길잡이 106 토마토 '
        '(90쪽) — 농사로 AI 이삭이 농업백과 경유'),
    ('cherry_tomato', 'harvest'): (
        '과실이 70% 이상 착색됐는지, 표면이 균일한 붉은색인지, 병든 과실은 '
        '아닌지 상품성을 본다. 탱탱하고 밝은 과실을 두 손으로 살짝 비틀어 '
        '수확하고, 수확 후 잔가지·웃자란 순을 정리한다.',
        '작물별 시비처방 기준(개정증보판, 67쪽)·농업기술길잡이 106 토마토 '
        '(90쪽) — 농사로 AI 이삭이 농업백과 경유'),

    # ── 토마토 ────────────────────────────────────────────────────
    # ⚠ 'seedling' 지침은 두지 않는다 — 육묘는 대개 이 구획이 아닌 다른 곳에서
    # 하므로(growth_stage_resolver.STAGE_DURATION_MAP 의 anchor 설명 참조),
    # 이 구획의 첫 단계는 정식이다.
    #
    # 챗봇 질문에 우리 단계 어휘(정식→영양생장기→개화기→착과기→결실기→수확기)를
    # 그대로 넣어 답을 받았다 — 그래서 6단계가 어긋남 없이 1:1로 맞는다.
    ('tomato', 'transplant'): (
        '묘의 활착 여부(잎이 시들거나 색이 변하지 않는지), 하우스 내 온습도가 '
        '적정한지(18℃ 이상 유지), 토양 수분(과습·건조 여부)을 본다. 정식 전 '
        '포장에 퇴비·유기물을 넣어 흙과 섞고, 점적관·비닐 멀칭을 설치하며 정식 '
        '구덩이에 충분히 관수한다. 묘를 심고 뿌리가 토양에 밀착되도록 눌러 주며, '
        '활착 초기 1~2주는 물을 듬뿍 주고 이후 잎이 시들 때 추가 관수한다.',
        '2010 토마토 유기재배 매뉴얼(55쪽)·농업기술길잡이 106(개정판) 토마토 '
        '(32쪽) — 농사로 AI 이삭이 농업백과 경유'),
    ('tomato', 'vegetative'): (
        '줄기·잎의 건강 상태(도장·웃자람, 병해충 발생), 곁순이 너무 자라지 '
        '않는지, 하우스 환기 상태와 습도·온도의 과다 변동을 본다. 곁순(측지)을 '
        '제거하고 주기적으로 웃거름을 주며 관수량을 조절한다. 지주대를 세우거나 '
        '줄로 유인(직립유인법)하고, 늙은 잎·하엽을 순차적으로 정리한다.',
        '2010 토마토 유기재배 매뉴얼(55쪽)·농업기술길잡이 106(개정판) 토마토 '
        '(32쪽) — 농사로 AI 이삭이 농업백과 경유'),
    ('tomato', 'flowering'): (
        '1~2화방의 개화 시기와 꽃 색·개화 상태를 보고, 꽃 색이 불량하거나 '
        '기형화가 없는지 확인한다. 착과율을 높이려면 진동기로 중진동 인공수분을 '
        '하거나(또는 이른 아침 손 진동) 곁순 제거와 아래 잎 정리를 이어간다.',
        '2010 토마토 유기재배 매뉴얼(55쪽)·농업기술길잡이 106(개정판) 토마토 '
        '(32쪽) — 농사로 AI 이삭이 농업백과 경유'),
    ('tomato', 'fruit_set'): (
        '어린 과실의 형성 상태와 신선도, 병해(과실썩음·줄기 이상)와 해충(담배 '
        '가루이 등) 유무, 과실 기형·착과 불량을 본다. 한 화방에 4~5개 과실만 '
        '남기고 나머지는 솎아 준다. 착과 후에는 충분히 관수하되 수분 과다 시 '
        '배수를 관리하고, 필요하면 미숙한 웃거름을 준다.',
        '2010 토마토 유기재배 매뉴얼(55·56쪽) — 농사로 AI 이삭이 농업백과 경유'),
    ('tomato', 'fruiting'): (
        '과실 크기와 색 변화(녹색→붉은색), 양분 결핍과 병반·병해(곰팡이·꽃썩음 '
        '등)를 본다. 익는 과실 가까이 있는 늙은 잎·병든 잎을 제거하고, 병든 '
        '과실과 곁가지를 제거하며 가지치기를 유지한다.',
        '2010 토마토 유기재배 매뉴얼(56쪽) — 농사로 AI 이삭이 농업백과 경유'),
    ('tomato', 'harvest'): (
        '과실 색(녹색→적색기)과 상품성(크기·모양·흠집), 병해충 흔적·썩음 '
        '발생을 본다. 색상 기준(녹색·변색·채색·도색·담적색·적색기)에 따라 '
        '단계별로 수확하고, 병든 과실과 나머지 잎 등을 제거하며 청결을 '
        '관리한다.',
        '2010 토마토 유기재배 매뉴얼(56쪽) — 농사로 AI 이삭이 농업백과 경유'),

    # ── 파프리카 ──────────────────────────────────────────────────
    # ⚠ 'seedling' 지침은 두지 않는다 — 육묘는 대개 이 구획이 아닌 다른 곳에서
    # 하므로(growth_stage_resolver.STAGE_DURATION_MAP 의 anchor 설명 참조),
    # 이 구획의 첫 단계는 정식이다.
    #
    # 챗봇 질문에 우리 단계 어휘(정식→영양생장기→개화기→착과기→결실기→수확기)를
    # 그대로 넣어 답을 받았다 — 그래서 6단계가 어긋남 없이 1:1로 맞는다.
    ('paprika', 'transplant'): (
        '모 상태와 뿌리 활착 여부(포기 쓰러짐, 잎색, 시듦 등), 정식 포장의 청결 '
        '(잡초·기계적 상해 흔적 등), 고정 유인줄 설치 여부를 본다. 모를 정식 '
        '깊이에 맞춰 심어 배지·토양에 빈 공간 없이 밀착시키고, 양액공급관·공간을 '
        '소독한다(차아염소산나트륨 100배액 등). 정식 후 유인줄을 설치해 흔들림을 '
        '막고, 활착 후에는 초기 병해충 예방용 약제를 관주한다.',
        '농업용 클로렐라 품목별 현장활용 방법(76쪽)·현장 데이터 수집을 위한 '
        '주요 시설채소 생육조사 매뉴얼(86쪽) — 농사로 AI 이삭이 농업백과 경유'),
    ('paprika', 'vegetative'): (
        '초장·잎 수·줄기 굵기·잎색의 발달 상태(모가 크고 힘있게, 잎이 넓고 '
        '연한색을 유지하는지), 병해충 발생(특히 진딧물·담배가루이), 과도한 '
        '절간 신장(환경·급액 점검 필요)을 본다. 온·습도를 관리하고(주간 '
        '25~30℃, 야간 17~20℃), 급액량·EC를 활착 후 3.0dS/m까지 단계적으로 '
        '조정하며, 가지를 유인하고 잎을 정리(하엽)하고 환경 변동 시 환기· '
        '냉방으로 대응한다.',
        '농업용 클로렐라 품목별 현장활용 방법(76쪽)·현장 데이터 수집을 위한 '
        '주요 시설채소 생육조사 매뉴얼(86쪽) — 농사로 AI 이삭이 농업백과 경유'),
    ('paprika', 'flowering'): (
        '첫 꽃이 개화되는 위치(방아다리 아래 완전 전개엽 수를 센다), 꽃 크기· '
        '기형화·낙화탈화율, 측지 발생 상태와 잎의 활력을 본다. 꽃대를 유인하고 '
        '필요하면 개화 부위의 하엽을 제거하며, 목표 착과 위치를 정한 뒤 배지 '
        '함수율·급액을 조정한다(잎이 처지거나 꽃이 떨어지지 않게). 균등한 '
        '착과를 유도하려면 필요시 약한 꽃을 솎는다.',
        '농업용 클로렐라 품목별 현장활용 방법(76쪽)·현장 데이터 수집을 위한 '
        '주요 시설채소 생육조사 매뉴얼(86쪽) — 농사로 AI 이삭이 농업백과 경유'),
    ('paprika', 'fruit_set'): (
        '열매(씨방)가 정상적으로 붙었는지 결실률을 확인하고, 절간 길이와 세력, '
        '꽃·어린 과실 상태, 열매 형태 이상(꼬리달림과·기형과 등)을 본다. 착과 '
        '후에는 급액량·EC를 재점검해 급격한 변화를 막고, 과실 하중에 맞춰 '
        '가지를 유인 관리하며, 배꼽썩음과·흰가루병 등 병해충을 예찰한다.',
        '농업용 클로렐라 품목별 현장활용 방법(76쪽)·현장 데이터 수집을 위한 '
        '주요 시설채소 생육조사 매뉴얼(86쪽) — 농사로 AI 이삭이 농업백과 경유'),
    ('paprika', 'fruiting'): (
        '과실 비대 상태(크기·색 변화 등), 미세열과·배꼽썩음과 같은 생리장해, '
        '잎색·잎세와 생식·영양의 균형을 본다. 배지 함수율을 65%로 유지하며 '
        '급액량을 조절하고, 염화칼슘 등 필요한 영양을 엽면시비하며, 과실 균형을 '
        '위한 솎음 등 병해충 방제를 지속한다.',
        '농업용 클로렐라 품목별 현장활용 방법(76쪽)·현장 데이터 수집을 위한 '
        '주요 시설채소 생육조사 매뉴얼(86쪽) — 농사로 AI 이삭이 농업백과 경유'),
    ('paprika', 'harvest'): (
        '과실 착색 균일성과 크기(적색·황색 완숙 상태), 과실 외관 손상과 질병 '
        '감염 여부, 과실 당도와 저장성을 본다. 과피 손상 없이 두 손이나 전용 '
        '가위로 적기 수확하고, 수확 후 예냉 처리를 하며 이물질·손상과를 '
        '선별하고 수확 기록을 남기며 위생적으로 포장한다.',
        '(수확후 품질관리기술 매뉴얼) 파프리카(11쪽) — 농사로 AI 이삭이 '
        '농업백과 경유'),

    # ── 오이 ──────────────────────────────────────────────────────
    # ⚠ 'seedling' 지침은 두지 않는다 — 육묘는 대개 이 구획이 아닌 다른 곳에서
    # 하므로(growth_stage_resolver.STAGE_DURATION_MAP 의 anchor 설명 참조),
    # 이 구획의 첫 단계는 정식이다.
    #
    # 챗봇 질문에 우리 단계 어휘(정식→영양생장기→개화기→결실기→수확기, 오이는
    # fruit_set 이 없다)를 그대로 넣어 답을 받았다 — 그래서 5단계가 어긋남 없이
    # 1:1로 맞는다.
    ('cucumber', 'transplant'): (
        '묘의 활착 상태(잎·줄기의 시듦·광택), 초기 병해충(덩굴마름병·뿌리혹병 '
        '등) 증상 유무, 지온·기온·토양 수분 정도를 본다. 활착을 촉진하는 관수를 '
        '하고 필요하면 보온·차광 조치를 하며, 뿌리와 줄기를 직접 점검(뿌리혹 '
        '등 이상증상 확인)한다. 기초 지주를 설치하고 초기 웃거름(밑거름 이후)을 '
        '준다.',
        '2008 국내외 농작업 편이장비·개인보호구 편람1(54쪽)·농업기술길잡이 '
        '107(개정판) 오이(33쪽) — 농사로 AI 이삭이 농업백과 경유'),
    ('cucumber', 'vegetative'): (
        '줄기 굵기·잎 크기·전체 초세(생육 상태), 잎·줄기 병해(노균병·바이러스 '
        '등)와 해충 유무, 하우스 내 온·습도 유지 상태를 본다. 줄기를 정돈해 '
        '추가로 유인·고정하고, 필요하면 가지치기·적심 작업을 하며 웃거름(관비 '
        '등)을 주고 잦은 관수를 조절한다.',
        '2008 국내외 농작업 편이장비·개인보호구 편람1(54쪽)·농업기술길잡이 '
        '107(개정판) 오이(33쪽) — 농사로 AI 이삭이 농업백과 경유'),
    ('cucumber', 'flowering'): (
        '암꽃·수꽃의 발달과 개화 상태, 수정 여부(열매 착생 확인), 꽃과 어린 '
        '과실 주변의 병해 유무를 본다. 필요하면 인공수분을 하고 상품이 안 될 '
        '개체의 꽃을 제거하며, 병든 꽃과 초기 낙과·과실을 제거하고 추가 유인과 '
        '수분(양액·관수) 공급을 한다.',
        '2008 국내외 농작업 편이장비·개인보호구 편람1(54쪽)·농업기술길잡이 '
        '107(개정판) 오이(33쪽) — 농사로 AI 이삭이 농업백과 경유'),
    ('cucumber', 'fruiting'): (
        '과실 비대 상태와 착색·변형(흑반·기형 등), 병해충(덩굴쪼김병·노균병 '
        '등)과 잎·줄기 상태, 과실 품질(길이·직경, 선첨과 여부)을 본다. 상품이 '
        '안 되는 과실은 제거해 비대를 조절하고, 적기에 관수하며 비료를 추가로 '
        '주고, 병해를 방제하며 필요하면 봉지 재배를 한다.',
        '2008 국내외 농작업 편이장비·개인보호구 편람1(54쪽)·농업기술길잡이 '
        '107(개정판) 오이(33쪽) — 농사로 AI 이삭이 농업백과 경유'),
    ('cucumber', 'harvest'): (
        '과실 크기와 상품성(수확 적기 판단), 과실·줄기·잎의 병해·이상 유무, '
        '하우스 내부 환경(온습·통풍)을 본다. 상품과 위주로 적기에 수확하고 '
        '선별·포장·운반하며, 수확 후 병든 식물체를 제거·폐기한다.',
        '2008 국내외 농작업 편이장비·개인보호구 편람1(54쪽)·농업기술길잡이 '
        '107(개정판) 오이(33쪽)·농작업일정 오이(4쪽) — 농사로 AI 이삭이 '
        '농업백과 경유'),

    # ── 딸기 ──────────────────────────────────────────────────────
    # ⚠ 'seedling' 지침은 두지 않는다 — 육묘는 대개 이 구획이 아닌 다른 곳에서
    # 하므로(growth_stage_resolver.STAGE_DURATION_MAP 의 anchor 설명 참조),
    # 이 구획의 첫 단계는 정식이다.
    #
    # 챗봇 질문에 우리 단계 어휘(정식→영양생장기→화아분화기→개화기→결실기→
    # 수확기)를 그대로 넣어 답을 받았다 — 그래서 6단계가 어긋남 없이 1:1로 맞는다.
    ('strawberry', 'transplant'): (
        '묘의 신선도와 병해충 감염 유무(탄저병·시들음병·뿌리썩음 등), 뿌리가 '
        '흙에 밀착돼 활착이 잘 되었는지, 심는 깊이(관부가 반쯤 묻혔는지)와 묘의 '
        '쓰러짐 여부를 본다. 묘 뿌리를 넓게 펴서 정식하고, 약한 겨드랑눈(곁눈)을 '
        '제거하며, 정식 후 충분히 관수한다.',
        '농업용 클로렐라 품목별 현장활용 방법(100쪽)·친환경농산물 생산을 위한 '
        '바닷물의 농업적 활용 매뉴얼(35쪽) — 농사로 AI 이삭이 농업백과 경유'),
    ('strawberry', 'vegetative'): (
        '잎 색상(짙고 건강한 녹색인지), 새 잎 상태와 생육 상태(초장·엽장·엽수 '
        '등을 정기 관찰), 병해충·생리장해(잎 마름·시듦·비대 불량 등)를 본다. '
        '불필요한 런너를 제거하고, 통기성을 위해 묵은 잎을 정리(적엽)하며 '
        '포장 내 잡초를 뽑고 점적관수 등으로 수분·양분을 관리한다.',
        '농업용 클로렐라 품목별 현장활용 방법(100쪽)·친환경농산물 생산을 위한 '
        '바닷물의 농업적 활용 매뉴얼(35쪽) — 농사로 AI 이삭이 농업백과 경유'),
    ('strawberry', 'flower_initiation'): (
        '꽃눈 분화 여부(최초 소화가 40% 이상 피었는지 관찰)와 영양생장·생식생장 '
        '균형(잎과 꽃눈의 균형 상태)을 본다. 화방 방향을 조정하고, 꽃눈 분화를 '
        '촉진하도록 보온·온도관리와 환경을 조성한다.',
        '농업용 클로렐라 품목별 현장활용 방법(100쪽)·친환경농산물 생산을 위한 '
        '바닷물의 농업적 활용 매뉴얼(35쪽) — 농사로 AI 이삭이 농업백과 경유'),
    ('strawberry', 'flowering'): (
        '개화율(화방별 전개 꽃 개수), 꽃가루 정상 여부와 벌 활동, 병꽃·기형화 '
        '등 이상 증상을 본다. 벌통 위치를 조정하고(지상 70cm, 남북·동서 방향을 '
        '모두 고려), 얕게 남은 액아(곁눈)를 정기적으로 제거하며, 수분 곤충을 '
        '투입하거나 인공수분으로 보조한다.',
        '농업용 클로렐라 품목별 현장활용 방법(100쪽)·친환경농산물 생산을 위한 '
        '바닷물의 농업적 활용 매뉴얼(35쪽) — 농사로 AI 이삭이 농업백과 경유'),
    ('strawberry', 'fruiting'): (
        '과실 비대 상태(과장·과폭), 착색률(90% 착색 이상 여부), 기형과·열과 '
        '등 품질 문제를 본다. 적과(불량과 제거)를 하고 부실한 꽃·과실과 하엽을 '
        '정리하며, 비대기에는 과도한 수분 공급을 피해 물·비료양을 조절한다.',
        '농업용 클로렐라 품목별 현장활용 방법(100쪽)·친환경농산물 생산을 위한 '
        '바닷물의 농업적 활용 매뉴얼(35쪽) — 농사로 AI 이삭이 농업백과 경유'),
    ('strawberry', 'harvest'): (
        '수확 적기(과실 표면이 90% 이상 착색됐는지), 과실 당도·경도(외관과 '
        '손맛으로 확인), 병든 과실·손상 과실을 구분해 본다. 과실이 손상되지 '
        '않게 살짝 비틀어 수확하고, 수분·상처를 막을 포장재를 준비하며, 수확 '
        '후 남은 병든 잎·과실을 처분하고 청결을 유지한다.',
        '농업기술길잡이 40(개정판) 딸기(210쪽) — 농사로 AI 이삭이 농업백과 '
        '경유'),

    # ── 상추 ──────────────────────────────────────────────────────
    # ⚠ 'seedling' 지침은 두지 않는다 — 육묘는 대개 이 구획이 아닌 다른 곳에서
    # 하므로(growth_stage_resolver.STAGE_DURATION_MAP 의 anchor 설명 참조),
    # 이 구획의 첫 단계는 정식이다.
    #
    # 챗봇 질문에 우리 단계 어휘(정식→생육기→수확기, 상추는 3단계뿐)를 그대로
    # 넣어 답을 받았다 — 그래서 3단계가 어긋남 없이 1:1로 맞는다.
    ('lettuce', 'transplant'): (
        '모종 활착 상태(파종 후 정식 7~10일 이내 잎이 선명하게 펼쳐지고 생장점 '
        '손상·시들음·변색이 없는지)와 토양·주변 위생(과습·과건 여부, 비닐 멀칭 '
        '상태, 병원균 매개체가 될 유기 잔사물 제거 상태)을 본다. 정식 간격은 '
        '잎상추 기준 15×15cm 또는 15×25cm로 맞추고, 완숙 유기질 퇴비로 밑거름을 '
        '정식 10일 전까지 주며, 멀칭을 설치하고 점적관수를 준비한다. 균핵병· '
        '노균병·시들음병은 정식 후 초기에 주로 발생하니 병든 모종은 즉시 '
        '제거하고 과습하지 않게 환기·점적관수로 관리한다.',
        '상추 유기종자 생산매뉴얼(25쪽)·상추 GAP 생산 가이드(13쪽) — 농사로 '
        'AI 이삭이 농업백과 경유'),
    ('lettuce', 'vegetative'): (
        '잎의 생육 상태(정상 생장·길이·색, 위축·곱슬림·반점 등 병해 증상 여부), '
        '토양 수분(포장용수량의 60~80% 유지, 너무 젖거나 마르지 않는지), 시설 '
        '환경(내부 온도 여름 25~30℃·겨울 15℃ 내외, 습도, 차광망 여름 35% '
        '이하 적용 여부, 환기팬 작동)을 본다. 잎 상태를 점검해 병든 잎을 즉시 '
        '제거하고, 정기적으로 관수하며(봄 5~6일, 가을 7~8일 간격), 비료를 '
        '추비한다(결구상추는 1~2회, 잎상추는 필요 시 추가 시비). 진딧물·배추 '
        '좀나방·뿌리혹선충·노균병·잿빛곰팡이병 등을 예찰하고 환기팬·측창 개폐로 '
        '습도·공기 순환을 관리한다.',
        '상추 유기종자 생산매뉴얼(25쪽)·상추 GAP 생산 가이드(13쪽) — 농사로 '
        'AI 이삭이 농업백과 경유'),
    ('lettuce', 'harvest'): (
        '수확 가능 잎 크기(12~15cm일 때 적기), 병든 잎·포기 여부(수확 전 반드시 '
        '제거), 수확 용기·장갑 등 위생 상태를 본다. 겉잎부터 3~4잎을 남기고 '
        '바깥 큰 잎부터 손톱으로 순차 채취하고, 병해충 감염 잎·포기는 분리해 '
        '폐기하며, 수확 후 신속히 예냉·저온보관해 신선도를 지킨다. 생육 '
        '후반부에는 잿빛곰팡이병·균핵병이 집중 발생할 수 있어 수확 전후 하우스 '
        '내부 환기·습도관리가 필요하고, 작업 전후 손 씻기·장갑 착용 등 위생을 '
        '지킨다.',
        '농작업 유해요인 개선 방안(39쪽)·상추 GAP 생산 가이드(13쪽) — 농사로 '
        'AI 이삭이 농업백과 경유'),

    # ── 시금치 ────────────────────────────────────────────────────
    # 시금치는 직파(直播)라 파종한 자리가 곧 이 구획이다 — 'seedling' 을 그대로
    # 첫 단계로 둔다(growth_stage_resolver.STAGE_DURATION_MAP 의 anchor 설명
    # 참조). 챗봇 질문에 우리 단계 어휘(파종→생육기→수확기)를 그대로 넣어 답을
    # 받았다.
    ('spinach', 'seedling'): (
        '파종량과 깊이(10a 기준 3~6L, 기계파종은 1~3L 권장, 깊이 약 1~2cm), '
        '토양 상태(pH 6.5~7.5, 과습·건조 주의, 씨앗이 고르게 묻혔는지), 발아 '
        '적온(15~20℃, 최저 4℃ 이상)을 본다. 종자를 파종하고(흩어뿌림·점파· '
        '조파·씨드테이프 등) 가볍게 눌러준 뒤 물을 준다(과습은 줄락병 위험이 '
        '있어 분수테이프·스프링클러를 쓴다). 발아 후에는 관수를 조절해 과습을 '
        '막고(노균병 예방), 필요하면 얇은 비닐·부직포로 피복한다.',
        '농업기술길잡이 204(신간) 시금치(30쪽) — 농사로 AI 이삭이 농업백과 '
        '경유'),
    ('spinach', 'vegetative'): (
        '본엽 1~2매 때 솎음이 필요한지, 포기 간격(주간 10~15cm 권장), 잎 앞· '
        '뒷면의 병해(노균병 — 황색 반점·보라색 곰팡이, 흰가루병 — 흰 균사)를 '
        '본다. 잎·줄기 생육 상태와 초장(20cm 전후)·색상도 함께 살핀다. '
        '기계파종은 본엽 1~2매 때 1회, 관행은 2회 솎아주고, 본엽 3~4매·5~6매 '
        '때는 질소 위주로 웃거름을 준다(표준시비 10a 기준 N 25kg·P₂O₅ '
        '5.9kg·K₂O 11.9kg). 병든 잎·포기는 조기에 제거하고 병해가 처음 '
        '보이면 바로 약제를 쓰며(흰가루병은 증상이 심하면 고추용 약제도 '
        '쓸 수 있다), 배수로를 정비하고 고온·저온기에 맞춰 피복·환기를 '
        '관리한다.',
        '농업기술길잡이 204 시금치(28쪽) — 농사로 AI 이삭이 농업백과 경유'),
    ('spinach', 'harvest'): (
        '초장이 20cm 내외로 자랐는지(품질·상품성 기준), 병해엽·노엽 상태와 '
        '결속에 적합한지(황화엽·병엽은 제거)를 본다. 포기째 뿌리를 포함해 '
        '수확하고(대량은 기계수확, 소규모는 인력수확), 황엽·병엽을 제거한 뒤 '
        '결속하며(1단 150~200g), 세척해 폴리 포장으로 출하한다(1박스 '
        '4.5~6kg).',
        '농업기술길잡이 204 시금치(91쪽) — 농사로 AI 이삭이 농업백과 경유'),
}


def _guidance_for(crop_key, stage_key):
    """`(지침, 출처)` — 없거나 출처가 비면 `(None, None)`.

    출처 없는 지침을 조용히 통과시키면 화면과 AI 는 그것을 조사된 지침으로 읽고,
    나중에 그 말이 어디서 왔는지 되짚을 길이 없다. 그래서 **거절이 아니라
    누락**이다 — 시드가 실패해 카탈로그 전체가 안 나오는 것보다, 근거 없는 한
    줄만 빠지는 편이 낫다.
    """
    entry = _STAGE_GUIDANCE.get((crop_key, stage_key))
    if not isinstance(entry, (tuple, list)) or len(entry) != 2:
        # 한 짝이 아니면 출처가 없다는 뜻이다 — 본문만 적어 넣은 실수를 여기서
        # 걸러야 그 한 줄이 출처 없이 화면과 AI 로 나가지 않는다.
        return None, None
    text, source = entry
    text = (text or '').strip()
    source = (source or '').strip()
    if not text or not source:
        return None, None
    return text, source


def _apply_guidance(crop_key, stages):
    """단계 목록에 지침을 얹고, 쓰인 출처 목록을 함께 돌려준다.

    빈 지침은 **키를 남기지 않는다** — `program_io._clean_stages` 와 같은 규약이다
    (있는데 비었다로 읽히면 화면이 빈 칸을 그린다).
    """
    sources = []
    for st in stages:
        text, source = _guidance_for(crop_key, st['key'])
        if not text:
            continue
        st['guidance'] = text
        if source not in sources:
            sources.append(source)
    return sources


def _stages_from_cumulative(pairs):
    """[(key, 누적일), …] → [{key, name, days}] (days = 그 단계의 길이)."""
    out, prev = [], 0
    for key, cum in pairs:
        if cum >= 999:
            days = None            # 끝까지 — 사람이 끝낸다
        else:
            days = max(int(cum) - prev, 0)
            prev = int(cum)
        out.append({
            'key': key,
            'name': _STAGE_NAMES.get(key, key),
            'days': days,
            # P1 은 단계·기간까지다. 목표·자원·GDD 는 이후 단계에서 채운다 —
            # 빈 dict 를 넣어 두면 화면이 "있는데 비었다" 로 읽는다.
        })
    return out


def _category_stages(members):
    """소속 작물들의 `STAGE_DURATION_MAP` 을 합쳐 카테고리 스켈레톤을 만든다.

    **여기서 새 값을 지어내지 않는다** — 이미 있는 하드코딩 표를 통계로
    요약할 뿐이다:

    1. 소속 작물마다 단계 길이를 구한다(`_stages_from_cumulative`).
    2. 소속들이 실제로 갖고 있는 단계 키를 합친다(합집합) — 예를 들어 딸기만
       가진 화아분화기도 "과채류" 카테고리에 나타난다.
    3. 단계 순서는 `_STAGE_NAMES` 에 이미 있는 생육 순서로 정렬한다.
    4. 그 단계를 가진 소속들의 길이 **중앙값**을 그 단계의 일수로 쓴다(소속
       하나만 그 단계를 가지면 중앙값은 그 값 그대로다 — 표본이 하나뿐이라는
       뜻이고, 그만큼 대표성이 약하다는 것도 사실이다).
    5. 마지막 단계는 소속 전부가 그렇듯 항상 `days=None`("끝까지")이다.

    **이 값은 대표값이지 정답이 아니다.** 카테고리로 만든 프로그램은 사람이
    화면에서 실제 대상에 맞게 고치는 것을 전제로 한다(그래서 `notes` 에
    그렇게 적어 둔다 — `catalog()` 참조).
    """
    from aot.ai.context.growth_stage_resolver import STAGE_DURATION_MAP

    per_member = {}
    for m in members:
        lengths = {st['key']: st['days']
                  for st in _stages_from_cumulative(STAGE_DURATION_MAP[m])
                  if st['days'] is not None}
        per_member[m] = lengths

    keys = set()
    for lengths in per_member.values():
        keys.update(lengths.keys())
    ordered_keys = sorted(keys, key=lambda k: _STAGE_ORDER.get(k, 999))

    out = []
    for key in ordered_keys:
        lengths = [per_member[m][key] for m in members if key in per_member[m]]
        out.append({
            'key': key,
            'name': _STAGE_NAMES.get(key, key),
            'days': int(round(statistics.median(lengths))),
        })
    # 마지막 단계 — 소속 작물 전부가 "끝까지" 로 끝나므로 카테고리도 그렇다.
    out.append({'key': 'harvest', 'name': _STAGE_NAMES.get('harvest', 'harvest'),
               'days': None})
    return out


def catalog():
    """템플릿 목록 → `[{key, name, subject, kind, scope, stages, …}]`.

    두 층을 이어 붙여 반환한다 — **카테고리가 먼저, 작물종이 그 다음**이다:

    - `scope='category'`: 넓은 범주(과채류 등). `members` 에 소속 작물종 키
      목록을 담아, 화면이 "또는 정확한 작물로 시작" 을 이어 보여줄 수 있게 한다.
      소속이 없는 카테고리(근채류 등, `_CATEGORY_MAP` 참조)는 만들지 않는다.
    - `scope='species'`: 기존의 작물종별 항목. `category` 필드로 소속 카테고리
      키를 담는다(없으면 `None`).

    모든 항목은 `kind='vegetation'` 이다 — 이 카탈로그는 `STAGE_DURATION_MAP`
    (작물 표) 하나만 읽으므로 다른 종류(`GeoProgram.kind` 의 livestock·facility·
    other)를 지어내지 않는다. 소비처는 자기 종류만 고른다는 원칙을 카탈로그
    쪽에서도 그대로 따르는 것 — DB 컬럼 기본값에 기대지 않고 여기서 명시한다.

    출처는 두 하드코딩 표다 — `STAGE_DURATION_MAP`(단계·기간)과
    `_CROP_PRESETS`(광합성 파라미터·권장 목표). **여기서 값을 다시 적지 않는다**:
    두 곳에 적으면 반드시 갈린다. 카테고리의 단계 일수도 이 표를 통계로 요약한
    것이지 새로 적은 값이 아니다(`_category_stages` 참조).

    ⚠ **목표(`targets`)는 채우지 않는다** (2026-08-19 되돌림). 잠깐 광합성 프리셋의
    작물 단위 값을 모든 단계에 복사했는데, 그것은 **단계별 값이 아니다** — 육묘기와
    착과기의 목표가 같을 리 없고, 같은 값을 여섯 칸에 채워 두면 사람은 그것을
    "조사된 추천값" 으로 읽는다. 채워진 숫자는 빈 칸보다 강한 주장이다.

    단계별 목표는 **실제 조사로** 채운다(작물별 재배 지침·시험 자료). 그 전까지는
    비워 두고, 사람이 자기 재배 방식대로 적는다.

    단계 지침(`guidance`)은 `_STAGE_GUIDANCE` 가 정본이다. 자리와
    규율만 있다 — **출처가 없으면 실리지 않는다.** 지침은 범위 검사가 없어 이
    저장소에서 가장 지어내기 쉬운 필드이고, 재배 지침서를 그대로 옮겨 적는 것은
    저작권 문제이기도 하다. 그 내용이 필요하면 `ext/nongsaro_client.py`(농사로
    오픈API)가 출처가 붙은 채로 받아 온다. 실린 지침이 있으면 그 출처가
    `guidance_sources` 로 함께 나가고, 프로그램을 만들 때 `source_note` 에 남는다.
    """
    from aot.ai.context.growth_stage_resolver import STAGE_DURATION_MAP
    try:
        from aot.scripts.seed_crop_presets import _CROP_PRESETS
    except Exception:
        _CROP_PRESETS = {}

    out = []

    # ── 카테고리 ──────────────────────────────────────────────────────
    # 소속이 하나도 없으면 만들지 않는다 — 근거 없는 스켈레톤은 빈 칸보다 나쁘다.
    for cat_key, cat in _CATEGORY_MAP.items():
        members = [m for m in cat['members'] if m in STAGE_DURATION_MAP]
        if not members:
            continue
        member_names = '·'.join(_CROP_NAMES.get(m, m) for m in members)
        out.append({
            'key': 'cat_%s' % cat_key,
            'name': cat['name'],
            # subject 는 비울 수 없으므로(저장 규칙) 카테고리 이름을 기본값으로
            # 채운다 — 사람이 그 자리에서 실제 대상 이름으로 고쳐 쓴다.
            'subject': cat['name'],
            'kind': 'vegetation',
            'scope': 'category',
            'category': None,
            'members': members,
            'stages': _category_stages(members),
            # 카테고리에는 지침을 붙이지 않는다(_STAGE_GUIDANCE 주석 참조) —
            # 소속 5종을 뭉뚱그린 자리라 어느 작물의 지침도 그 범위의 것이 아니다.
            'guidance_sources': None,
            'photosynthesis': None,
            'notes': ('카테고리 대표값 — %s종(%s)의 단계 일수 중앙값입니다. '
                      '실제 재배 전 단계·일수·목표를 반드시 실제 대상에 맞게 '
                      '고치세요.' % (len(members), member_names)),
        })

    # ── 작물종 ────────────────────────────────────────────────────────
    for crop, pairs in STAGE_DURATION_MAP.items():
        if crop.startswith('_'):
            continue
        preset = _CROP_PRESETS.get(_PRESET_ALIAS.get(crop, crop)) or {}
        stages = _stages_from_cumulative(pairs)
        # 지침은 출처가 있는 것만 실린다(_guidance_for). 지금은 `_STAGE_GUIDANCE`
        # 가 갖는다(출처 없는 지침은 실리지 않는다).
        guidance_sources = _apply_guidance(crop, stages) or None

        # 조사된 목표가 있으면 얹는다(`crop_target_sources`). **없으면 비워 둔다** —
        # 자료가 없는 작물에 그럴듯한 숫자를 채우면 사람은 그것을 조사 결과로 읽는다.
        defs, note = _apply_targets(crop, stages)

        # 광합성 파라미터는 **작물 단위 모델 상수**라 단계와 무관하므로 그대로
        # 싣는다 — 그쪽은 채워도 거짓이 아니다.
        photo = {k: v for k, v in preset.items()
                 if k not in ('display_name', 'notes')} or None
        out.append({
            'key': crop,
            'name': _CROP_NAMES.get(crop, crop),
            'subject': crop,
            'kind': 'vegetation',
            'scope': 'species',
            'category': _SPECIES_CATEGORY.get(crop),
            'target_defs': defs,
            'stages': stages,
            # 이 템플릿의 지침이 어디서 왔는가. 프로그램을 만들 때 `source_note`
            # 로 따라간다(routes_geo_plot.api_program_create) — 지침만 남고 출처가
            # 사라지면, 나중에 그 말을 고칠 사람이 판단할 재료가 없다.
            'guidance_sources': guidance_sources,
            'photosynthesis': photo,
            'notes': note,
        })
    return out


def _apply_targets(crop, stages):
    """조사된 목표를 얹는다 → (target_defs|None, notes|None).

    `stages` 는 **제자리에서** 고친다(그 단계에서만 다른 값을 `targets` 로).
    작기 내내 같은 값은 항목 정의의 기본값(`default`)으로 올라간다 — 단계마다
    되풀이하면 조사해서 단계별로 다르게 정한 것처럼 보이고, 고칠 때 한 곳만
    고쳐진다.

    **출처를 `notes` 에 싣는다.** 템플릿에서 만든 프로그램에도 그대로 남아, 나중에
    이 숫자를 고칠 사람이 근거를 볼 수 있다.
    """
    try:
        from aot.scripts.crop_target_sources import SOURCES, SPECIES_TARGETS
        from aot.aot_flask.geo.program_io import fixed_target_defs
    except Exception:
        return None, None

    spec = SPECIES_TARGETS.get(crop)
    if not spec:
        return None, None

    # 지침은 여기서 다루지 않는다 — `_apply_guidance` 가 `_STAGE_GUIDANCE` 를
    # 정본으로 삼아 따로 얹는다(출처가 없으면 싣지 않는 규율이 그쪽에 있다).
    for st in stages:
        vals = (spec.get('stages') or {}).get(st['key'])
        if vals:
            st['targets'] = dict(vals)

    defs = fixed_target_defs('vegetation')
    for d in defs:
        if d['key'] in (spec.get('defaults') or {}):
            d['default'] = spec['defaults'][d['key']]
        d['fixed'] = True
        d['hidden'] = False

    src = SOURCES.get(spec.get('src')) or {}
    note = None
    if src:
        note = '목표 출처: %s' % src.get('title', spec.get('src'))
        if src.get('note'):
            note += ' — %s' % src['note']
        if src.get('url'):
            note += ' (%s)' % src['url']
    return defs, note


def purge_builtin(app):
    """예전 시드가 깔아 둔 `source='builtin'` **식생** 프로그램을 걷어낸다.

    `kind='vegetation'` 으로 좁힌다 — `GeoProgram` 은 가축·시설물과 테이블을
    같이 쓰므로, 종류를 안 걸면 이 시드가 모르는 다른 종류의 내장 프로그램까지
    건드리게 된다.

    **참조 중인 것은 남긴다** — 지우면 그 작기가 "무엇을 목표로 길렀나" 의 근거를
    잃는다(program_io.delete_program 과 같은 규칙).
    """
    from aot.aot_flask.extensions import db
    from aot.databases.models import GeoProgram, GeoPlot

    removed, kept = [], []
    with app.app_context():
        for row in GeoProgram.query.filter_by(source='builtin',
                                               kind='vegetation').all():
            used = GeoPlot.query.filter_by(program_uuid=row.unique_id).count()
            if used:
                kept.append((row.name, used))
                continue
            removed.append(row.name)
            db.session.delete(row)
        db.session.commit()
    return removed, kept


def main():
    ap = argparse.ArgumentParser(description='재배 프로그램 템플릿 카탈로그')
    ap.add_argument('--list', action='store_true', help='카탈로그 보기')
    ap.add_argument('--purge-builtin', action='store_true',
                    help='예전에 깔린 내장 프로그램 제거(참조 중인 것은 남김)')
    ap.add_argument('--json', action='store_true', help='기계 판독')
    args = ap.parse_args()

    if args.list or not args.purge_builtin:
        items = catalog()
        if args.json:
            print(json.dumps(items, ensure_ascii=False))
        else:
            cats = [it for it in items if it['scope'] == 'category']
            species = [it for it in items if it['scope'] == 'species']
            print('카테고리 %d 종 + 작물종 %d 종 (DB 에 넣지 않는다 — 화면에서 '
                  '고를 때 만들어진다)' % (len(cats), len(species)))
            for it in cats:
                print('  - [범주] %-20s %s (%d단계, 소속 %d종)'
                      % (it['key'], it['name'], len(it['stages']),
                         len(it['members'])))
            for it in species:
                has_t = any(st.get('targets') for st in it['stages'])
                n_g = sum(1 for st in it['stages'] if st.get('guidance'))
                cat = (' · 소속 %s' % it['category']) if it['category'] else ''
                print('  - %-14s %s (%d단계%s%s%s)'
                      % (it['key'], it['name'], len(it['stages']),
                         ', 목표 있음' if has_t else '',
                         ', 지침 %d단계' % n_g if n_g else '', cat))
                for src in (it.get('guidance_sources') or []):
                    print('      출처: %s' % src)
        if not args.purge_builtin:
            return 0

    try:
        from aot.start_flask_ui import app
    except Exception as exc:                       # pragma: no cover
        print('앱 로드 실패: %s' % exc, file=sys.stderr)
        return 2
    try:
        removed, kept = purge_builtin(app)
    except Exception as exc:
        print('정리 실패: %s' % exc, file=sys.stderr)
        return 2
    if args.json:
        print(json.dumps({'removed': removed,
                          'kept_in_use': [{'name': n, 'plots': c}
                                          for n, c in kept]}, ensure_ascii=False))
    else:
        print('제거 %d · 사용 중이라 남김 %d' % (len(removed), len(kept)))
        for n in removed:
            print('  - %s' % n)
        for n, c in kept:
            print('  ! %s (구획 %d 건이 사용 중)' % (n, c))
    return 0


if __name__ == '__main__':
    sys.exit(main())
