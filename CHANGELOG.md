# AoT — 변경 이력

AoT는 오픈소스 환경 제어 시스템을 기반으로,  
**AI 오케스트레이션**과 **벡터 기반 GIS** 를 통합한 스마트팜 특화 플랫폼입니다.

---

## 플랫폼 주요 특징 (Platform Overview)

### 벡터 기반 GIS
타일 기반이 아닌 벡터 렌더링으로 부드럽고 자연스러운 지도를 제공합니다.

- **GeoMap**: OSM / 위성 등 다양한 맵 프로바이더, 도형 그리기, 장치 배치
- **GeoFacility + 3D Widget**: 시설(온실/하우스) 외형을 폴리곤으로 정의하고 3D로 시각화
- **자동 액추에이터 제어**: 환기창(vent), 커튼(curtain), 측창(side window), 도어, 조명 등 시설 구성 요소를 GIS 폴리곤에 연결해 자동 제어
- **풍향 차등 제어**: 강풍 시 windward(풍상) 개구부만 강제 폐쇄, leeward(풍하) 환기 유지
- **GeoShape 기반 효과 산출**: 개구부 면적·방위각을 폴리곤에서 자동 계산해 제어 효과에 반영
- **시설 센서 바인딩**: 실내/외 온도·습도·CO₂·풍향 센서를 시설에 역할별로 연결

### 통합 환경 제어 (Layer 3 Coordinator)
PI 제어 기반의 다중 액추에이터 조율 시스템입니다.

- **ActuatorProfile**: GIS 폴리곤에서 azimuth·면적을 자동 산출해 프로필 구성
- **SafetyPreGate**: 강풍·강우·폭염·혹한 안전 관문 (하드 제약, 롤백 보장)
- **예측 피드포워드**: 외부 기상 예보 기반 선행 제어
- **광합성 모델**: Big-Leaf 모델(A_max, K_L, T_opt, VPD_half)로 작물 맞춤 목표값 산출
- **누적 추적**: DLI(일적산광량), GDD(적산온도), VPD, CO₂ 일별 누적 관리
- **보정 시스템**: 센서·액추에이터 오차 캘리브레이션

### AI beta: 테스트 중인 버전입니다. 간략한 대화와 안내가 가능합니다.
사용자가 직접 AI API 키를 등록해 **조언형(Advisory)** 또는 **챗봇형(Chat)** AI를 구성할 수 있습니다.

- **다중 AI 제공자 지원**: Anthropic(Claude), Google(Gemini), OpenAI, Mistral, Groq, Ollama(로컬 LLM), MiniMax
- **파이프라인 역할 분리**: Router → Planner → Executor → Synthesizer → Worker 계층 구조
- **Tier 기반 분류기**: Tier0 사전분류(0-토큰) → Tier1 라우팅 → Tier2 실행으로 응답 속도 최적화
- **MCP(Model Context Protocol) 서버**: AI가 시설 상태 조회(Observe), 이상 분석(Diagnose), 설정 변경(Control)을 도구 호출로 수행
- **사용자 승인 게이트**: 쓰기 도구(설정 변경)는 60초 내 사용자 승인 필요, 감사 로그 90일 보존

### AI 지식 레이어
AI 학습에 필요한 지식을 사용자가 직접 구성할 수 있는 레이어 시스템입니다.

- **AIContextSource**: REST API / 문서 / 웹 URL / 내부 쿼리 등 외부 지식 소스 등록 및 주기적 동기화
- **AIDomainGlossary**: 스마트팜 도메인 용어사전 (pending/active 상태 관리)
- **AIFacilityLearning**: 시설별 패턴 학습 및 모델 버전 관리
- **AIUserProfile**: 사용자별 선호 에이전트 및 피드백 설정
- **AISummary / AIRecommendation**: 일간 환경 요약 및 AI 권장사항 기록
---

# 미출시 (Unreleased)

## Docker 배포판 업데이트 — 알림부터 자동 설치까지

Docker 로 배포한 시스템에는 업데이트 경로가 없었습니다. 업그레이드 페이지는 붉은
경고 한 덩어리로 "호스트에서 알아서 하라" 고만 말했고, 더 나쁜 것은 **알림 자체가
틀렸다**는 점이었습니다. 업데이트 가용 여부를 GitHub 릴리스 태그로 판정했는데,
태그는 밀자마자 생기고 이미지는 멀티아치 빌드가 끝나야 받을 수 있습니다. 그 사이
"새 버전 있음" 이 뜨지만 실제로 받으려 하면 없습니다.

- **판정 기준을 레지스트리로 교체**: Docker 에서는 GHCR 에 **실제로 발행된 이미지**
  목록을 봅니다. 움직이는 태그(`latest`, `YY.MM`, `sha-*`)는 릴리스로 세지 않습니다 —
  가리키는 대상이 조용히 바뀌는 값을 버전 비교에 넣을 수 없습니다. 조회에 실패하면
  "업데이트 있음" 이 아니라 오류를 냅니다.
- **원클릭 업데이트**: 앱은 자기 자신을 업데이트할 수 없습니다(`docker compose up -d`
  가 그 명령을 실행한 컨테이너까지 교체해 명령이 중간에 죽습니다). 그래서 별도
  업데이터가 백업 → 이미지 교체 → 검증을 수행하고, 새 버전이 뜨지 않으면 이전
  버전으로 되돌립니다. 업데이터는 선택 설치이며, 켜지 않으면 지금처럼 호스트에서
  직접 하되 화면에는 정확한 상태가 표시됩니다.
- **되돌리기는 이미지와 데이터를 함께**: 새 컨테이너가 뜨는 순간 스키마가 올라가고
  그 되돌림은 대체로 구현돼 있지 않습니다. 이미지만 이전 것으로 바꾸면 **미래
  스키마 위에서 옛 코드가 도는** 상태가 됩니다. 그래서 확인 절차가 버전과 스키마
  리비전을 함께 보고, 이미지만으로 회복되지 않으면 업데이트 직전 백업까지
  복원합니다. 실제로 마이그레이션 후 실패·다운그레이드 시도 두 경우를 일부러
  만들어 끝까지 돌려 확인했습니다.
- **지정한 시각에 자동 설치**: 설정 > 업그레이드에서 토글을 켜고 시각을 넣으면 매일
  그 시각에 확인해 설치합니다. 기본은 꺼짐입니다. 판단은 데몬이, 실행은 업데이터가
  합니다 — 권한을 가진 쪽을 최대한 단순하게 두기 위해서입니다.
  **컨테이너가 다시 뜨는 몇 분 동안 제어가 멈추므로, 중요한 작동이 없는 시각을
  고르십시오.** 작동 중이면 연기하는 기능은 아직 없습니다.
- **시간대**: 입력한 시각은 현지 시각입니다. 시간대를 읽는 기존 도우미는 웹 요청
  문맥이 없으면 조용히 UTC 로 물러서는데 데몬에는 그 문맥이 없어, 그대로 뒀다면
  한국 사용자가 03:00 을 넣고 정오에 업데이트를 맞았을 자리였습니다.
- **헬스 엔드포인트 추가**(`/health`): 컨테이너 교체 중 "새 빌드가 실제로 서빙하는가"
  를 묻는 자리입니다. 로그인 없이 얻을 수 있는 정보는 생존 여부뿐이고, 버전과 스키마
  리비전은 인증이나 전용 키를 요구합니다.

## 마이그레이션이 조용히 건너뛰어지던 문제 — 체인 검사기

업그레이드는 `head` 가 아니라 `config.ALEMBIC_VERSION` 상수를 목표로 실행됩니다
(폐기된 계보가 두 번째 head 로 남아 있어 `head` 를 그대로 쓰면 실패하기 때문).
그래서 마이그레이션을 추가하고 이 상수를 올리지 않으면 **그 마이그레이션은 영원히
실행되지 않습니다.** 에러도 경고도 없습니다 — 앱은 "up to date" 를 로그에 남기고
정상 기동하고, 모델과 라우트는 존재하지 않는 컬럼을 참조한 채 돕니다.

두 번 났고 두 번 다 사람이 눈으로 찾았습니다. 같은 검사가 테스트에 이미 있었지만
**테스트 스위트는 커밋·릴리스 시점에 돌지 않아** 두 번 다 놓쳤습니다. 없었던 것은
검사가 아니라 검사가 도는 시점이었습니다.

- **체인 검사기 추가**: 뿌리 단일성 · 끊어진 부모 · head 단일성 · 상수 일치 4종을
  점검합니다. 표준 라이브러리만 쓰고 앱 설정을 import 하지 않아, 설치가 깨져 있어도
  검사 자체는 돕니다.
- **커밋 시점에 차단**: pre-commit 훅이 인덱스(스테이지된 내용)를 검사합니다.
  워킹트리를 보면 "마이그레이션만 스테이지하고 상수 수정은 빠뜨린" 커밋을
  통과시켜 버리는데, 그것이 정확히 막으려던 경우입니다.
- **CI 관문 추가**: 훅을 켜지 않은 클론과 우회 커밋도 병합 전에 걸립니다.
- **구현 일원화**: 기존 테스트에 중복돼 있던 같은 로직을 걷어내고 검사기에
  위임하도록 바꿨습니다. 판정 기준이 두 벌로 갈라지지 않습니다.

---

# v26.08.2 (2026-08-03 ~ 2026-08-05) — 지도 데이터 무결성 계층 · 지도 위젯 팝업 일원화 · 조용히 실패하던 경로 일괄 정리

이번 판의 절반은 **조용히 실패하던 것들**입니다. 에러도 로그도 없이 기대와 다르게
동작해 온 경로들 — 지도 도형의 종류가 두 곳에 따로 저장돼 갈라지던 문제, 콜드
스토리지가 import조차 안 되던 상태, 티어 이동이 "옮겼다"고 거짓 보고하던 문제,
긴급정지 보류를 우회할 수 있던 경로 — 을 찾아 고치고, 같은 종류가 다시 생기지
않도록 DB 계층과 검사기로 막았습니다.

## 지도 데이터 무결성 (S1~S5 · P1~P4)

도형의 '종류'가 `GeoShape.type` 과 `feature.properties.aot_type` 두 곳에 중복
저장돼 있었고, `type` 에는 `default='feature'` 라는 조용한 폴백이 있었습니다.
도형을 만드는 코드가 `type` 을 한 번 빠뜨리면 에러 없이 `'feature'` 가 들어가고,
그 뒤 저장은 "기존 것 없음"으로 판정해 UPDATE 대신 INSERT 합니다 — 저장 한 번에
같은 도형이 두 벌이 되는데 아무 에러도 나지 않습니다. 읽는 경로마다 기준이 달라
부분적으로만 망가진 채 몇 주씩 굴러가다, 업그레이드로 읽기 경로가 바뀌는 순간
"예전 시설이 지도에서 사라졌다"로 표면화됩니다.

- **무결성 검사기 추가**: `type-mismatch` · `duplicate` · `dangling-link` ·
  `orphan-facility` · `orphan-label` 5종을 읽기 전용으로 점검합니다. 업그레이드
  전후로 돌리면 사람이 화면에서 발견하기 전에 배포가 스스로 알려줍니다.
- **불변식을 DB 계층에 강제**: 어긋난 도형 타입과 끊어진 zone/site 소속을 일괄
  복구한 뒤, Tier-1·Tier-2 불변식을 마이그레이션으로 적용했습니다(p6_22·p6_23).
  `aot_type` 을 단일 정본으로 삼아 되먹임 고리를 끊었습니다.
- **쓰기 경로를 게이트웨이로 좁힘**: 배치 게이트웨이와 소유권 검사를 두어 우회
  경로를 봉쇄했습니다.
- **지도 소유권 모델 정비**: 지도 자동 생성을 폐지하고, 장치의 지도 소속을 별도
  컬럼이 아니라 **배치에서 파생**하도록 바꿨습니다. '지도 종류' 개념도 함께
  폐기했습니다. 전환 과정에서 드러난 회귀(미배치 장치가 모든 지도에 나타나던 문제,
  파생이 미사용 사본을 고르던 문제)도 같이 고쳤습니다.
- **함께 고친 결함**: 장치 1개 삭제가 공유 디자인 지도를 통째로 지우던 문제,
  지도 복제가 원본과 얽히던 결함 3종(장치 배치·계층·node_id), 명시 삭제 경로가
  막혀 있던 회귀.

## 지도 위젯 — 팝업·라벨 일원화

같은 장치라도 어디서 열었느냐에 따라 다른 창이 뜨고, 라벨이 서로 가려도 규칙을
알 수 없던 상태를 정리했습니다.

- **입력 값 키를 시설 방식으로 통일**: 지도·구역에 배치된 입력도 시설 안의 센서와
  똑같이 측정 구간 색상·다채널 포맷·마커 스타일을 따르고, 같은 설정을 읽습니다.
  이를 위해 서버가 측정 채널에 구간 판정용 키를 붙입니다 — 화면 표시명은 번역돼
  나가므로 판정 기준으로 쓸 수 없었고, 그래서 지도 경로는 구간 색상 자체가
  불가능했습니다. 표시 단위도 종류별 표준 단위를 덮어씌우지 않고 실제 저장 단위를
  씁니다(Pa 로 저장한 VPD 에 kPa 라벨이 붙던 문제).
- **출력 시작/종료 예약을 공용 창으로**: 구역·시설 모달과 장치 마커 팝업이 같은
  창을 씁니다. 시작이 미래면 **서버 스케줄러에 등록**되어 브라우저를 닫아도
  실행됩니다 — 이전에는 열어 둔 탭이 대기하는 방식이라 탭을 닫으면 사라졌습니다.
- **그래프 통일**: 팝업마다 따로 있던 차트 구현을 공용 렌더러 하나로 모았습니다.
  축·툴팁·레전드가 화면마다 미묘하게 다르던 것이 사라집니다.
- **라벨 쌓임·겹침 규칙 정리**: 쌓임 순서를 한 표로 고정하고(대지 > 구역 > 시설 >
  장비 > 출력 > 입력 > 함수), 겹칠 때는 반대로 **구체적인 대상이 남습니다**.
  밀려난 라벨은 `이름 +N` 배지로 드러나 조용히 사라지지 않습니다. 마우스를 올리거나
  클릭한 라벨은 종류와 무관하게 맨 앞으로 옵니다.
- **확대 수준에 따른 라벨 숨김**(신규 옵션, 기본 16): 이보다 축소하면 시설·출력·
  입력·함수 라벨을 숨깁니다. 대지·구역 라벨은 위치를 가늠할 수 있도록 유지됩니다.
- **입력 값 로딩 970ms → 45ms**: 라벨이 실제로 쓰는 채널만 조회하도록 바꾸고(전
  채널을 받으면서 정작 라벨에서 제외하는 통신 상태 채널까지 받고 있었습니다),
  마지막 값을 세션에 캐시해 새로고침 직후 대기 없이 그리며, 측정값 조회를 건별
  질의에서 **일괄 질의**로 바꿨습니다. 세 가지를 합쳐 실측 기준 약 20배입니다.

## 환경 제어 (env-coordinator)

- **긴급정지 보류를 즉시 실행 명령으로 우회할 수 있던 문제 수정.**
- **능동 탐색이 장치에 전송되지 않던 문제**와 외부 컨텍스트 폴백 미작동 수정.
- **작물 프리셋 동기화 실패가 debug 로 묻혀 보이지 않던 문제 수정.**
- 관수 겸용 분무기를 환경 제어 대상에서 뺄 수 있게 했습니다.
- 사이클 함수를 분해하고(727줄 → 444줄) 조율기 파일군에 타입 힌트를 도입했습니다.

## 스토리지 · 데이터베이스

- **콜드 스토리지가 import 조차 안 되던 상태 해소** — 테이블을 신설했습니다.
- **티어 이동이 실제로는 옮기지 않고도 "옮겼다"고 보고하던 문제 수정**, warm 티어
  테스트가 돌지 않던 문제도 함께 고쳤습니다.
- **AI 가 문서 스토리지 티어를 조회·기록**할 수 있는 도구 7종을 추가했습니다.
- DB 조회 실패 시 반환 타입이 달라 엉뚱한 곳에서 터지던 문제를 반환 타입 통일과
  호출부 가드로 정리했습니다.

## 그 외 수정

- **DailyMultiPoint 설정점 곡선이 로드조차 안 되던 회귀 수정.**
- **MCP HTTP 서버가 Flask 개발 서버로 돌던 문제 수정** — 운영 서버로 전환했습니다.
- **승인 게이트의 레이트 리밋이 프로세스마다 따로 세던 문제 수정**, 승인 유효시간도
  분리했습니다.
- 백업에서 `node_modules`·`__pycache__` 를 제외해 용량이 58% 줄었습니다.
- 백업 관리 페이지가 베어메탈 설치에서 경로 불일치로 죽던 문제 수정.
- 인메모리 SQLite 에 풀 크기 인자를 넘겨 앱 생성이 죽던 문제 수정.
- 지도 위젯 설정 문서를 실제 옵션과 대조해 정리했습니다(ko/en).

## 업그레이드 시 주의

- **스키마 마이그레이션이 포함됩니다**(p6_22 · p6_23 · p6_25). 업그레이드 전후로
  `python3 -m aot.scripts.check_geo_integrity` 를 돌려 지도 데이터 상태를 확인하는
  것을 권합니다 — 읽기 전용이라 운영 중에도 안전합니다.
- 이전 판에서 도형 종류가 어긋난 채 저장된 데이터가 있다면 이번 판의 복구
  마이그레이션이 바로잡습니다. 중복 도형이 검출되면 **기하가 같다는 이유로 임의로
  지우지 마세요** — 어느 쪽에 시설·장치 참조가 매달려 있는지는 기하가 알려주지
  않습니다. 검사기 출력의 참조 정보를 먼저 확인하세요.

---

# v26.08.1 (2026-08-03) — 육묘 일소 잠금 폴백 정상화 · 번역 373건 복구 · 업그레이드 브랜치 경로 정정

v26.08.0 에서 들어간 육묘 일소 방지의 후속 수정과, 번역이 채워져 있는데도 화면에는 영문으로 나오던 문제를 묶은 패치 판입니다. 육묘장을 운용 중이라면 업그레이드를 권합니다.

## 환경 제어 (env-coordinator)

- **육묘 일소 잠금이 일사 센서를 어느 수집기에 물렸느냐로 갈리던 문제 수정**: 독립된 두 원인이 겹쳐 있었다. (1) 게이트 환경값 조립부와 `ext_context_collector` 가 일사 항목을 기본값 `0.0` 으로 채워, 「실내 추정 광량 → 실외 일사 → 태양고도 어림값」 3단 폴백의 중간 단이 늘 0.0 으로 메워졌다. 그래서 어림값에 영원히 도달하지 못했고, 일사 센서가 없는 육묘장은 v26.08.0 에서 폴백을 넣은 뒤에도 감쇠만 받고 **하드 잠금은 죽은 채 정오를 맞았다**. 이제 기본값을 채우지 않고, 실외 일사는 양수일 때만 측정값으로 인정한다 — 대낮의 0.0 은 센서 고장이며 그때 어림값으로 잠그는 편이 일소 보호에서 안전한 방향이다. (2) 실외 일사의 유입구가 둘(시설 도면의 실외 센서, `ext_context_collector`)인데 실내 광량 추정이 전자만 받아, 같은 물리 상황에서도 센서를 어디에 물렸느냐로 판정이 갈렸다 — 실외 700 W/m², 차광막 완전폐쇄(투과율 0.3), 잠금 임계 250 에서 한쪽은 실내 210 으로 보고 잠그지 않고 다른 쪽은 700 으로 보고 잠갔다. 두 유입구 모두에 차광 개도를 적용하고, 시설 실외 센서가 있으면 더 가까운 실측인 그쪽을 우선한다. 회귀 테스트 18건 추가.
- **함께 살아나는 동작**: 위 시설에서는 실내 추정 광량 자체가 만들어지지 않아 광량 하드 임계(`light_max`·`light_min`)도 조용히 죽어 있었다. 시설 실외 센서를 쓰는 경우와 같은 동작이 되므로 방향은 맞지만, 해당 시설에 임계를 설정해 뒀다면 **이번 판부터 실제로 발동**한다.

## 번역 (i18n)

- **한국어·일본어 번역 178건이 조용히 영문으로 나오던 문제 수정**: `pybabel update` 는 msgid 가 정확히 일치하지 않으면 문자열이 비슷한 다른 항목의 번역을 빌려 와 `#, fuzzy` 로 표시하는데, 이렇게 표시된 항목은 `pybabel compile` 이 .mo 에서 통째로 빼 버린다. .po 파일만 보면 번역이 채워져 있어 보여서 알아채기 어렵다. 한국어 78건·일본어 100건이 이 상태였다 — 빌려온 번역이 맞는 항목은 플래그만 떼고(ko 14 / ja 13), 나머지는 다시 번역했다(ko 64 / ja 87). SenseCAP 기상관측 입력이 가장 심해서 `Soil Temperature*` 5종이 전부 "색온도"로, `Rain Gauge` 가 "회전 각도"로 나오고 있었고, `Soil Moisture 10~40cm` 는 네 깊이가 모두 "토양 수분" 한 가지로 뭉개져 구분되지 않았다. 그 밖에 `Backup deleted`("백업 복원"), `Timezone`("타임라인"), `Privacy Policy`("LoRa 클래스 정책"), AI 채팅 컴포저의 응답 깊이·자율 수준 세그먼트, 캘린더 위젯의 보기 이름 등이 포함된다.
- **fuzzy 검사기가 한 번도 돌지 못하던 원인 제거**: `aot/scripts/check_i18n_fuzzy.py` 가 polib 파싱 오류로 즉시 죽었다. 시설 페이지 안내 문구의 큰따옴표가 ko·ja .po 에서 이스케이프되지 않은 채 들어가 있었던 것으로, PO 문법 위반이다. babel 파서는 관대해 넘어갔기 때문에 화면 표시에는 문제가 없었고 검사기만 막혀 있었다. 이제 검사기가 정상 종료한다(ko·ja fuzzy 0건).
- **중국어(간체·번체) 195건도 같은 방식으로 정리**: ko·ja 와 겹치는 100건에 더해, 통합환경제어(`_function_info.py`) 51건과 색상 설정 13건이 중국어에만 남아 있었다 — `Vent Actuation Profile` 이 "停用失败"(비활성화 실패), `Strawberry` 가 "Raspberry Pi", `Tomato` 가 "自动", `Effect Engine` 이 "有效U值" 로 나오던 식이다. 플래그만 제거 9건, 재번역 186건. 두 로케일 번역률이 90.6% → 94.6% 가 되었다. 간체·번체를 각각 작성했고 두 열의 한자 집합이 서로 섞이지 않는지 대조했다. **중국어 원어민 검토는 아직 받지 않았다** — 특히 이 카탈로그가 `Output`·`Setpoint`·`Value` 같은 기술 용어를 영문으로 두는 관행과, 캘린더 이벤트 본문 11건을 번역한 이번 판단이 어긋날 수 있다.

## 업그레이드

- **GitHub 기본 브랜치 참조를 main 으로 정정**: 저장소 기본 브랜치는 `main` 인데 인앱 수동 업그레이드(`FORCE_UPGRADE_MASTER`)가 받는 tarball 경로와 앱·문서의 소스 링크가 `master` 를 가리키고 있었다. GitHub 이 이 저장소에 `master → main` 별칭 리다이렉트를 걸어주고 있어 실제 고장으로 드러나지는 않았지만, 문서화되지 않은 별칭에 기대는 상태였다. 명시적 ref 로 바꿨다. 설정 이름 `FORCE_UPGRADE_MASTER` 와 하위 명령 `upgrade-master` 는 외부 문서에 이미 퍼져 있어 그대로 뒀다.

## 알려진 사항

- 나머지 20개 로케일(de fr hi hu it lt nl nn pl ru sr sv tr uk 각 1,256건 / es id pt th 각 195건)에는 fuzzy 가 그대로 남아 있다. 앞의 14개는 실효 번역률이 40.6% 라 fuzzy 를 고쳐도 절반이 영문이고, 빌려온 msgstr 이 해당 언어가 아니라 **다른 영문 문자열**인 경우가 1,256건 중 1,224~1,254건이다(예: 독일어 `Output Channel` → "Enable Output Controls"). 지금은 fuzzy 라서 원문 영어로 폴백되지만 플래그만 떼면 뜻이 다른 영어가 라벨에 박히므로, 손대지 않는 편이 안전하다.

---

# v26.08.0 (2026-07-31 ~ 2026-08-02) — 태양시 통합 · 시설 설계 페이지 전면 재편 · 육묘 일소 방지 · API 키 평문 노출 차단 · 의미 색상 사용자 지정 · 번들 드리프트 게이트

*(v26.07.5 태그 이후 준비만 하고 릴리스하지 못한 누적분을 하나로 묶습니다. 이전 판 문서에서 v26.07.4·v26.07.5 항목 아래 적혀 있던 태양시 통합과 분무 계열 환경 제어 항목은 실제로는 그 릴리스에 들어가지 않았습니다 — 이 판으로 옮겼습니다.)*

## 시설 설계 페이지 전면 재편

시설 하나를 처음 설정하려면 한 페이지에 세로로 쌓인 6개 섹션과 8개 탭 사이를
위아래로 네 번 왕복해야 했습니다. 3D 미리보기 위에는 오버레이가 여섯 겹 상시로
떠 뷰포트를 가렸고, 기능을 하나씩 붙여온 탓에 어디서 무엇을 하는지 알기
어려웠습니다. 골격부터 다시 짰습니다.

- **스테이지 + 6단계 드로어**: 3D/지도를 항상 보이는 자리(스테이지)에 두고, 설정은 6단계로 나눠 우측 드로어에서 연다. 드로어는 Input·Output·Function 페이지가 쓰는 공용 셸이라 백드롭 없이 본문을 밀어내며, 설정을 바꾸는 동안 3D를 계속 보고 조작할 수 있다. 스테이지는 단계에 따라 전환된다(위치·구조는 지도, 나머지는 3D). 좌측 툴 팔레트는 레일+플라이아웃으로 분해해, 뷰포트 절반을 덮던 관수 폼 세 벌이 열었을 때만 보인다. 하단 인스펙터를 드로어로 흡수해 뷰포트가 패널 전체 높이를 쓴다.
- **시설 선택·생성·저장이 페이지 이동 없이**: 기존에는 셋 다 페이지를 새로 열어, 드로어가 닫히고 보던 단계가 1단계로 돌아가고 3D 씬과 지도가 처음부터 다시 만들어졌다. 제자리에서 처리하도록 바꿔 작업 흐름이 끊기지 않는다.
- **설비 배치를 종류에 반응하는 구성요소 표로**: 단계 이름은 "설비 배치"인데 표는 센서만 다루고 있었다. 배치한 모든 구성요소를 다루되 행이 종류에 맞춰 바뀐다 — 센서는 측정 채널·측정 종류·실내외, 팬은 액추에이터+역할, 난방기·가습기·커튼·창문·측창·문은 액추에이터. 종류를 바꾸면 그 행이 즉시 다시 그려지고 3D 메쉬도 함께 바뀐다.
- **3D 화면에서 구성요소를 끌어서 옮기기**: 위치 지정 수단이 가느다란 축 화살표뿐이라 조준해야 잡혔고, 처음 보는 사람은 그것이 조작 대상인 줄도 몰랐다. 본체를 그대로 끌 수 있게 하되 그 구성요소가 놓인 높이의 수평면 위로만 움직이므로 실수로 높이가 바뀌지 않는다(높이는 축 화살표와 좌표 입력이 담당). 선택하지 않은 것도 바로 잡히고, 끌기 시작하면 선택되며 해당 행 아래로 배치 패널이 열린다.
- **외피가 만든 천창·측창·커튼도 선택·편집 가능하게**: 이들은 외피 설정에서 켜고 끄는 것 말고는 손댈 수 없었다 — 3D에서 클릭하면 선택은 되는데 아무 패널도 열리지 않았다. 편집 경로는 이미 다 있었고 UI만 빠져 있었다. 구성요소 표에 포함하고, 손으로 고친 항목에는 "수동 크기" 꼬리표와 "자동으로 되돌리기" 버튼을 붙였다(한 번 손대면 되돌릴 길이 없어 외피 기능을 통째로 껐다 켜는 수밖에 없었다).
- **함께 생성된 구성요소를 인스턴스로 — 크기·제어장치 공유**: 커튼·측벽커튼·측창은 그룹으로 묶여 있었는데 천창만 빠져 있어, 16베이면 16개를 하나씩 고쳐야 했다. 천창에 그룹을 부여하고 제어장치도 그룹 전파 대상에 넣었다(같은 모터를 16번 고르던 것). 크기·형상 연동과 제어장치 연동을 분리해 개별 해제할 수 있고, 표의 행이 "16개 연동" / "개별"을 표시해 한 줄을 고치면 몇 개가 함께 바뀌는지 알려준다.
- **함께 고친 결함**: 손편집한 외피 항목이 저장에서 빠지던 버그, 외피가 만드는 구성요소 이름이 영어로 나오던 문제, 대지 선택 드롭다운이 옛 목록을 붙들고 있어 선택이 안 되던 문제, 3D 미리보기 지면·격자가 시설 중심이 아니어서 시설이 한쪽으로 치우쳐 보이던 문제, 좁은 창에서 3D 미리보기가 213px까지 눌리던 문제, 크기·위치 칸에 한 글자 칠 때마다 포커스가 빠지던 문제.

## 환경 제어 (env-coordinator)

- **설비 팔레트로 배치한 가습기·난방기가 통합환경제어에 조용히 누락되던 버그 수정**: 시설 디자이너에는 가습기·난방기를 배치하고 Output 까지 물리는 배치 버튼과 종류 드롭다운이 있는데, 피팅 종류 → 액추에이터 종류 매핑 표에 이 종류들이 없어 등록 단계에서 그냥 버려졌다. 사용자는 가습기를 붙여 놨는데 통합환경제어에는 VPD 를 낮출 액추에이터가 하나도 잡히지 않는 상태가 된다. 실패가 조용해서(경고도 없음) 설정이 잘못된 것처럼 보인다. `fogger`·`heater`·`cooler`·`co2_injector`·`lighting` 을 매핑에 추가하고, 매핑 결과가 실제 액추에이터 종류 목록 안에 있는지 검사하는 테스트를 넣었다.
- **분무·관수 액추에이터를 통합환경제어에 연결 — VPD를 낮출 수단이 아예 없던 문제 해소**: 시설 도면의 관수 밸브·펌프는 env 액추에이터 등록에서 무조건 제외돼 있었고, 피팅 경로로 들어온 것들도 `ActuatorProfile.kind` 목록에 없는 이름으로 매핑돼 효과 모델이 빈 채로 등록됐다. 결과적으로 통합환경제어 쪽에는 습도를 올릴 수 있는 액추에이터가 하나도 잡히지 않아, VPD 관리를 별도 PID Function 으로 분리해 운영할 수밖에 없었다. 이제 노즐 구성으로 판정한다 — 분무 노즐이 달린 밸브·펌프는 `fogger` 로 등록하고, 드립 전용은 종전대로 제외한다.
- **노즐 배치에서 엽면 습윤 위험을 자동 산출**: 시설 디자이너가 저장하는 노즐 하나하나의 좌표·유량·살수 반경·분사 방향이 유량 합계 하나로 뭉개져 제어기에 전달됐다. 즉 제어기는 "물이 얼마나 나오는지"만 알고 "어디에, 어느 방향으로, 얼마나 굵게 떨어지는지"를 몰랐다. 살수 원을 격자 샘플링해 강수강도(mm/h)와 중첩도를 산출하고, 유량·반경·분사방향으로 습윤형/비습윤형(고압 미세포그)을 자동 판정해 제어 판단에 쓴다. 사용자가 노즐 사양을 따로 입력할 필요가 없고, 앞으로 설치되는 모든 시설이 같은 보호를 받는다.
- **관수식 펄스 도징 — 분무를 개도가 아니라 회당 정량 + 간격으로 제어**: 분무기를 0–100% 개도로 연속 변조하면 사이클의 절반을 계속 뿌리는 것과 같아 잎이 마를 틈이 없다. 습윤형 분무기는 1회 분무를 짧게 끊고 건조 시간을 강제하도록 바꿔, 가습량이 펄스 폭이 아니라 펄스 빈도로 조절되게 했다(관수가 일정 간격으로 정량을 주는 방식과 같다). 고압 미세포그와 드립은 종전대로 연속 변조.
- **일몰 전 분무 중단 옵션 — 야간 엽면 습윤에 의한 병해 예방**: 관수는 보통 일출·일몰 두 시간대에 하는데, 저녁 분무는 잎이 젖은 채로 밤을 넘기게 만든다. 엽면 습윤 지속 시간이 길수록 잿빛곰팡이·노균병 위험이 커지고 육묘장은 밀식이라 확산이 빠르다. 일몰 몇 분 전부터 분무를 중단하고 다음 일출까지 유지하는 옵션을 육묘장 모드에 추가했다. 작물에 따라 저녁 관수가 필요한 경우가 있어 기본은 허용이며, 이 설정은 습도 관리용 분무만 제어하고 같은 밸브에 걸린 별도 관수 일정에는 영향을 주지 않는다. 시설 좌표로 일몰을 계산하지 못하면 차단하지 않는다 — 위치를 모른다고 정상 가습까지 막으면 오히려 해롭다.
- **육묘장 모드 — 고일사 중 분무에 의한 일소 방지**: VPD 우선 제어에서 포그는 T↓·RH↑ 를 동시에 만들어 VPD 이득이 압도적으로 크다. 그래서 VPD 편차가 최대인 정오 — 곧 일사가 최대인 시각 — 에 조율자가 분무를 최대로 선택하는 구조적 공진이 있었고, 상토를 막 뚫고 나온 자엽에 물방울이 맺힌 채 최대 일사를 받아 일소가 발생했다(2026-07-31 aot-005 육묘장). 안전 프리게이트에 분무 잠금 관문을 신설해 실내 추정 광량이 임계 이상이면 습윤형 분무를 차단하고, 그 아래 구간은 선형 감쇠한다. 잠금은 폭염 게이트보다 우선하고(한여름 정오는 두 조건이 겹친다) `humid_min` 하드 임계의 강제 가습도 이긴다. 히스테리시스를 둬 구름이 지나갈 때마다 켜졌다 꺼지지 않으며, 분무 잠금 단독일 때는 부분 게이트로 동작해 나머지 제어는 정상 진행한다. 원수를 지하수로 설정하면 잠금 임계를 자동으로 더 보수적으로 적용한다 — 지하수는 경도가 높아 물방울이 마르며 염류를 농축시키고 수온이 낮아 저온 충격까지 겹치기 때문.
- **일사 센서 없는 시설에서 일소 보호가 반쪽이던 문제 수정**: 태양고도 기반 어림 일사가 계산은 되지만 안전 게이트에 전달되지 않아, 감쇠는 동작하는데 하드 잠금만 걸리지 않았다. 폴백을 넣은 목적이 정확히 그 경우라 보호가 절반만 작동한 셈이고, 조용히 실패하므로 로그로도 드러나지 않았다. 회귀 방지 테스트 3건을 함께 넣었고, 매뉴얼에 "통합 환경 제어 — 육묘장 모드" 절을 신설했다.

## 태양시(일출·일몰) 통합

농작업은 시계가 아니라 해에 묶여 있습니다. 관수·분무·차광·환기·조명은 모두
태양일을 따라 움직이는데, 지금까지 일출·일몰은 트리거 한 종류의 전용 계산이라
나머지 기능은 고정 시각을 쓸 수밖에 없었습니다. 계절이 바뀌면 사용자가 매번
시각을 다시 입력해야 했습니다. 태양시를 시스템 공용 기본값으로 끌어올렸습니다.

- **태양시 커널 신설**: 일출·일몰·남중·시민박명·낮 길이·태양고도를 한 곳에서 계산합니다. 위치는 새로 입력받지 않고 지도의 도형(사이트·구역)에서 상속하므로, 서로 다른 포장의 장치는 각자의 일출을 갖습니다. 좌표가 비어 있어도 상위 도형 → 농장 지도 중심 순으로 물려받아 동작합니다. 극지의 백야·극야는 오류가 아니라 상태로 표시합니다.
- **조건부에 태양 조건 2종 추가**: "지금 주간인가"(일출·일몰 오프셋으로 양 끝 조정)와 "다음 태양 이벤트까지 남은 시간"을 조건으로 쓸 수 있습니다. 위치는 조건부 기능에서 상속하므로 좌표를 따로 설정하지 않습니다.
- **LoRaWAN 운영시간을 일출~일몰 기준으로**: 성능(Class C) 모드 구간을 고정 시각 대신 그 장치 위치의 낮 길이에 맞출 수 있습니다. 04–18시로 고정해 두면 한여름에는 일출 후 한 시간 반이 지나서야 성능 모드로 올라가고, 일몰(19:38)보다 한참 이른 18시에 절전으로 내려갑니다.
- **설정점 곡선을 해에 걸 수 있음**: 하루 단위 메서드 구간의 양 끝을 태양 이벤트(±오프셋)에 걸 수 있습니다. 사인·베지어 곡선은 위상을 남중에 고정할 수 있어, 곡선의 마루가 시계가 아니라 해를 따라갑니다(남중은 같은 표준시대 안에서도 30분 넘게 움직입니다).
- **트리거가 태양 이벤트 5종 전체 지원**: 일출·일몰에 더해 남중·시민박명 시작/끝을 고를 수 있습니다. 박명 기준 조명·차광 제어에 씁니다. 실행 시각 표시도 "다음 실행" 한 줄로 정리했습니다(예전에는 일출·일몰 양쪽 시각을 나란히 보여줘 어느 쪽이 실제 실행인지 사용자가 골라야 했습니다).
- **AI가 태양시를 압니다**: 위치별 현재 시각 조회에 일출·일몰·남중·박명·낮 길이·주간여부·다음 경계까지 남은 시간이 함께 실립니다. "내일 일몰 30분 전에 밸브 열어줘" 같은 일회성 예약도 태양 기준으로 잡을 수 있습니다.
- **그래프 야간 음영(선택)**: 일몰~다음 일출 구간을 음영으로 표시해 측정값을 태양일 기준으로 읽을 수 있습니다. 기존 대시보드의 겉모습이 바뀌지 않도록 기본은 꺼져 있습니다.
- **일사 센서가 없는 시설도 육묘 일소 보호가 걸립니다**: 일소 게이트는 광량을 모르면 잠그지 않는데, 그 결과 일사 센서가 없는 시설은 보호가 통째로 꺼진 채 돌아갔습니다(정오에 분무가 그대로 나갑니다). 좌표만 있으면 태양고도로 맑은 날 기준 일사를 어림해 판정합니다. 측정값이 하나라도 있으면 어림값은 쓰지 않습니다.

### 함께 수정한 결함

- **설정한 시각과 다르게 동작하던 3건**: (1) LoRaWAN 운영시간과 (2) 하루 단위 설정점 곡선이 컨테이너 시간대(UTC)로 판정돼 한국에서 9시간 밀려 적용됐습니다. (3) 조건부의 조건 저장·삭제 버튼이 엉뚱한 폼으로 전송돼 모든 조건 타입에서 조용히 실패했습니다.
- **좌표가 없으면 일출/일몰 트리거를 저장조차 할 수 없던 문제**: 실행은 상위 도형에서 위치를 상속하는데 저장 검증만 좌표를 필수로 요구했습니다.
- 이 밖에 일출/일몰 계산이 선택 설치 의존성이라 미설치 시 조용히 멈추던 문제, 날짜 오프셋 설정이 무시되던 문제, 차트 공용 스크립트가 캐시 무효화 없이 배포돼 수정이 사용자에게 닿지 않던 문제를 함께 고쳤습니다.

## 보안

- **저장된 API 키가 권한 없는 사용자에게 평문으로 나가던 경로 차단**: API 키 피커가 페이지를 렌더링할 때 저장된 모든 키의 평문을 HTML 에 미리 심고 있었다. 근본 원인은 전역 컨텍스트 프로세서라, 옵션 id 에 `api_key`/`token` 이 들어간 필드가 하나라도 있는 화면(대시보드 위젯 포함)이면 그 페이지의 자체 권한과 무관하게 전 키가 박혔다. 읽기전용 기본 역할(Monitor)도 그 대상이었다. 피커를 "전량 미리 렌더링"에서 "선택한 키 하나만 그때 조회"로 바꾸고, 게이트를 `edit_settings`/`edit_controllers` 로 올렸다. 권한 체크가 통째로 빠져 있던 라우트(AI 에이전트 옵션, AI 라이브러리 소스 목록, GIS 설정 GET 등)도 형제 라우트 관례에 맞춰 보완했다. **지도 제공자 키는 여기서 끝나지 않는다** — 타일 URL 에 치환된 채 나가고 브라우저가 타일 서버를 직접 부르는 구조라 클라이언트 노출이 원리적으로 불가피하다. 이 계열은 제공자 콘솔의 referrer/도메인 제한이 유일한 방어다.
- **API 키 관리 페이지가 저장된 모든 키를 평문으로 실어보내던 구조 제거**: 목록이 키마다 편집 폼을 통째로 펼쳐놓고 본문을 `value` 속성에 그대로 렌더했다. 페이지를 열 수 있는 사람이면 소스만 봐도 저장된 크리덴셜 전부를 읽을 수 있었다. 목록을 카드+드로어로 바꾸고 키 본문을 목록에서 완전히 들어냈다 — 본문은 카드를 열 때 그 키 하나만 서버에서 받아온다. 드로어 안에서도 기본은 가려두고 [표시] 로만 드러낸다. 카드 드래그로 순서를 바꿀 수 있다.

## 사용자·역할 설정

- **사용자 설정을 카드+드로어로 — 목록에서 남의 정보가 사라집니다**: 한 페이지에 모든 사용자의 편집 폼이 펼쳐져 있어, 이메일·역할·키패드 상태·API 키 칸이 전부 페이지 HTML 에 실려 내려왔다. 어깨너머로 보이는 것도 문제지만 소스만 열면 전원의 정보가 그대로 보였다. 목록에는 이름·역할·상태만 남기고 상세는 열었을 때 서버에서 받아온다. 사용자/역할/일반 3탭으로 나누고, 카드 드래그로 순서를 바꿀 수 있다.
- **표시 이름 추가**: 로그인 계정명은 "koat"·"5739" 처럼 알아볼 수 없는 값이 굳어 있는 경우가 많다. 별도의 표시 이름을 두고, 비어 있으면 지금까지처럼 계정명을 보여준다.
- **계정 사용 스위치**: 계정을 지우지 않고 끌 수 있다. 비밀번호·키패드·Google 로그인과 API 키 인증 모두에서 막히고, 이미 열려 있던 세션도 다음 요청에 끊긴다. 본인 계정과 마지막 관리자는 끌 수 없다.

## 테마·색상

- **의미 색상(성공·경고·위험·정보)을 사용자 지정 가능하게**: 이 4색은 테마 변수 파일의 고정값이라 운영자가 바꿀 수 없었고, 그 때문에 페이지들이 "지정 가능한 변수"를 찾다가 축이 다른 색을 끌어다 쓰는 일이 생겼다(시설 페이지의 선택 행이 장치 상태색을 빌려 써 "켜져 있는 채널"처럼 보이던 문제). 4색 + 틴트 쌍 6개, 합 10필드를 사용자 지정 UI 에 추가하고 5개 내장 프리셋에 병합했다. 브랜드(선택·호버) / 의미(성공·경고·실패·진행) / 장치 상태(채널·카드)는 서로 대체재가 아니다 — 시설 페이지의 차용을 전부 원복했다.
- **다크 테마 사용자에게 의미색 4종 발행 생략**: 라이트 기준으로 계산된 값이 다크 화면에 그대로 실리던 문제.
- **상태 배지·태그·진행바·자산 카드를 공용 시트로 승격**: 시설 페이지에만 있던 것 중 그 페이지에 고유하지 않은 패턴을 공용 컴포넌트로 올렸다. 페이지 전용 클래스 111종 → 92종, 페이지 CSS 54KB → 50.7KB. 남은 것은 3D 툴 레일·스테이지 전환 등 이 페이지에서만 의미가 있는 구조다.

## 하드웨어 연동

- **RAK3172 밸브 컨트롤러의 배터리 전류 측정 종류가 잘못 등록돼 있던 문제 수정**: 측정 정의가 시스템에 등록되지 않은 종류 이름(`current`)을 쓰고 있었다. 등록된 이름(`electrical_current`)으로 정정했다. 이 값은 문서 생성 스크립트가 측정 종류를 조회하는 순간 크래시를 일으켜, 측정별 입력 목록 문서가 갱신되지 않던 원인이기도 하다.

## 개발·배포 인프라

- **JS 번들 드리프트 게이트**: 소스만 고치고 재빌드 없이 커밋하면 배포된 앱은 계속 옛 코드를 실행한다. 테스트도 정적 참조 검사도 못 잡는 조용한 실패다 — 실제로 지도·시설 위젯의 장치 두절 표시가 이 방식으로 4일간 죽어 있었고, 무관한 작업 중 우연히 재빌드하다 발견됐다. 빌드 그룹 3종(esbuild·rollup·vite)을 전부 등록한 검사기를 만들고, pre-commit 훅과 CI 게이트를 붙였다. 확정 판정 모드는 임시 트리에 실제로 재빌드해 바이트를 비교한다.
- **익명 통계 서버를 실제 보유 도메인 + TLS 로 전환**: 기존 수신 도메인은 상위 도메인이 레지스트리에 미등록 상태라 제3자가 등록해 가짜 수신 서버를 세울 수 있는 스쿼팅 위험이 있었다. 보유 도메인으로 옮기고 443/TLS(인증서 검증 포함)로 전환했다.
- **영문 매뉴얼이 한국어로 생성되던 문제 수정**: 매뉴얼 문서 생성 스크립트는 요청 컨텍스트 없이 도는데, 앱 컨텍스트가 이미 밀려 있고 시스템 언어가 영어가 아니면 UI 문자열이 그 언어로 풀려 영문 매뉴얼 안에 한국어가 섞여 들어갔다. 생성 시 로케일을 영문에 고정하는 공용 컨텍스트를 만들어 7개 생성 스크립트에 적용했고, 그동안 한국어로 굳어 있던 함수·액션·GIS 레이어·위젯 문서를 다시 생성했다.
- **죽은 지도 설정 라우트 제거**: 지도 위젯의 설정 옵션 조회 API 가 최초 커밋부터 정의된 적 없는 메서드를 불러 항상 500 을 반환했다. 호출하는 프론트엔드도 위젯 간소화 리팩터 이후 존재하지 않는 DOM 을 찾는 완전한 죽은 코드였다(실제 설정 모달의 드롭다운은 프레임워크 기본 메커니즘으로 이미 정상 동작). 라우트·스크립트·참조를 함께 걷어냈다.
- **GIS 삭제 경로 회귀 테스트 2건**: 설계지도 삭제가 반쯤만 진행돼 시설 행이 고아로 남던 사고(2026-07-26, 운영 설계지도 하나가 날아가 2주 전 백업으로 복구)와, 지도 편집기의 삭제 도구가 시설 외곽선을 지울 때 시설 행을 조용히 남기던 경로를 고정했다.

## 업그레이드 안내

- DB 마이그레이션 5건(p6_16 ~ p6_20)이 업그레이드 시 자동 적용됩니다 — 조건부 태양 조건, 설정점 곡선의 태양 앵커, 사용자·역할 카드 순서, 표시 이름·계정 사용 스위치, API 키 카드 순서.
- 기존 설치의 동작이 바뀌는 항목은 없습니다. 태양시·차광포 투과율·그래프 야간 음영·육묘장 모드는 모두 설정해야 켜집니다.
- **API 키 노출 수정 때문에** 읽기전용 역할(Monitor)은 이제 API 키 피커를 쓸 수 없습니다. 위젯·입력 설정에서 키를 골라야 하는 사용자에게는 편집 권한이 필요합니다.

---

# v26.07.5 (2026-07-28 ~ 2026-07-31) — 환경 제어 안전 결함 4건 수정 · 차광막 광량 맹점 해소 · LoRaWAN 다운링크 폭주 수정 · Output 저장 경로 정상화 · 공유 버스 측창 개폐기

여름철 현장 운용 중 드러난 환경 제어 안전 결함을 집중 수정한 판입니다. 개구부·차광막을 다루는 설비라면 업그레이드를 권합니다.

## 환경 제어 (env-coordinator)

- **무구배 전환 시 커튼·차광막이 예고 없이 닫히던 버그 수정**: 포화 구간의 anti-windup이 내부 적분값만 낮춰두는데, 이후 무구배(NO_GRADIENT) 전환 시 완화 로직이 그 숨겨진 낮은 값에서 감쇠를 시작해, 실제로는 열려 있던 커튼·차광막이 갑자기 닫히는 쪽으로 급락했다(폭염 중 보온커튼 오폐쇄로 실제 발현). 감쇠 시작점을 직전 실제 지령 위치로 변경.
- **온습도 하드 임계 오버라이드가 아예 동작하지 않던 문제 수정**: 강제 냉방·난방·제습·가습 플래그가 설정만 되고 소비되는 곳이 없어, `temp_max/min`·`humid_max/min` 하드 임계가 완전히 무효였다. 안전 게이트와 동일한 액추에이터 조합으로 실제 동작하도록 연결. 아울러 광부족(`light_min`) 시 보광등만 켜려 하고 차광막을 열어주는 대칭 로직이 없어, 보광등이 없는 가장 흔한 구성에서는 광부족에 아무 대응도 못 하던 문제도 수정.
- **임계 오버라이드가 안전 프리게이트를 덮어쓰던 안전 결함 수정**: 되살린 온습도 오버라이드가 안전 게이트의 강제 명령보다 뒤에 적용돼, 게이트가 풍상측 개구부를 닫아도 강제 냉방이 이를 다시 활짝 열었다. 한여름에는 실내 고온과 강풍이 동시에 오므로 실제 발생 가능한 조합이다. 적용 순서를 뒤집어 안전 게이트가 최종 권한을 갖게 하고, 리팩터링으로 순서가 조용히 깨지지 않도록 한 함수로 묶었다. 게이트는 자신이 명령한 액추에이터만 덮으므로 풍하측은 냉방을 위해 계속 열릴 수 있다.
- **하드 임계 판정 히스테리시스 추가 — 액추에이터 채터링 방지**: 임계 위반 판정이 단순 비교라 값이 경계에서 흔들리면 강제 오버라이드가 매 사이클 켜졌다 꺼졌다 했다(차광막이 14분 만에 개방→폐쇄를 왕복한 사례). 차광막·커튼은 안티채터 장치가 없는 데다 편도 수백 초짜리 full-stroke 주행이라 피해가 크다. 진입·해제 문턱을 비대칭으로 두는 래치를 도입(온도 0.5°C, 습도 2%, 광량은 임계의 10%).
- **차광포 투과율 입력 + 실내 광량 추정 — 차광막이 만든 광부족을 감지**: 실내 광센서가 없으면 실외 일사가 그대로 실내값으로 쓰이는데, 그 값은 차광막을 닫아도 변하지 않는다. 즉 차광막이 스스로 만든 광부족을 원리적으로 감지할 수 없었다. 개도를 반영해 실내 광량을 추정하도록 하고(통과율 = 1 − 폐쇄율 × (1 − 투과율), 여러 장이면 가장 어두운 쪽 채택), 투과율은 액추에이터별(액션)과 함수 기본값 양쪽에서 설정할 수 있다 — 시설 도면에서 자동 발견된 액추에이터는 액션 행이 없어 값을 넣을 자리가 없던 문제를 함수 옵션으로 해소. 투과율 미설정 시 추정하지 않으므로 기존 설치 동작은 그대로다(opt-in).

## 하드웨어 연동

- **다운링크 페이싱이 혼잡할 때 오히려 폭주하던 버그 수정**: 대기 상한(30초)이 대기 시간만 자르고 슬롯 예약은 그대로 진행해, 대기열이 7건을 넘으면 이후 전송들이 모두 30초를 기다린 뒤 **동시에** 발사됐다 — 속도 제한이 정확히 필요한 순간에 무력화되는 구조였다. 상한을 넘으면 슬롯을 잡지 않고 전송을 포기(상위에 실패 반환)하도록 수정. 전송 경로 3곳이 예외를 삼키고 그냥 전송하던 것도 공용 헬퍼 하나로 통일해 우회 경로를 없앴다.
- **확인 재전송이 자기 시한 뒤에 전파로 나가던 문제 수정**: 재전송 간격이 페이싱 간격(4초)보다 짧으면 타이머가 깨워도 전송은 페이싱에 막혀 대기하다가, 이미 실패 처리된 뒤에야 나갔다. SF12 기준 1.32초의 전파를 헛되이 쓰고 그 슬롯은 다른 장치가 필요로 하던 것이다. 재전송 간격 하한을 링크의 전송 제한에 맞추는 훅을 신설하고 ChirpStack 출력이 이를 오버라이드하도록 함(명령 시한 8초 기준 명령당 전송 3회 → 2회). 전송 제한이 없는 다른 출력 드라이버는 기존 하한 유지.

## 출력 (Outputs)

- **설정 저장이 "데몬에 연결할 수 없음"으로 실패하던 문제 수정**: 저장 시 데몬이 드라이버를 내렸다 올리는데, 그 과정이 장치 종류에 따라 수십 초까지 걸린다(LoRaWAN 전송 슬롯 대기 최대 30초, MQTT 리스너 종료 대기 5초). 웹에서 데몬을 부르는 쪽 상한은 10초라, 실제로는 저장이 진행 중인데도 화면에는 연결 실패로 표시됐다. 재적재를 백그라운드로 넘겨 즉시 응답하도록 수정(같은 장치의 연속 저장은 순서 보장).
- **설정 저장이 장치를 물리적으로 구동하던 문제 수정**: 드라이버 재적재 과정에서 종료 상태(Shutdown State)와 시작 상태(Startup State) 전송이 그대로 재생돼, 이름만 바꿔 저장해도 밸브가 실제로 움직였다. LoRaWAN에서는 저장 한 번에 다운링크 2건을 소모하기도 했다. 이 두 전송은 데몬을 켜고 끌 때의 동작이지 저장 시 할 일이 아니므로 저장 경로에서만 건너뛰도록 수정. 장치 **삭제** 시에는 종료 상태를 그대로 적용한다(지우기 전에 안전한 상태로 보내는 것이 맞음).
- **공유 버스 측창 개폐기 출력 모듈 신설**: 열림·닫힘 릴레이 1쌍을 여러 액추에이터가 공유하고 창마다 선택 릴레이로 버스를 물리는 하우스 측창 개폐기 배선을 지원한다. 6채널 릴레이 보드로 창 4개를 제어할 수 있다(CH1 열림, CH2 닫힘, CH3~CH6 선택). 신규 UI 문구 ko/ja 번역 포함.

---

# v26.07.4 (2026-07-20 ~ 2026-07-28) — 통합 시간대·Google Calendar 연동 · 지도 위젯 대개편 · 복합장치(Device) 티어 · 보안 강화(2FA·감사로그·API키 해시화) · Modbus PLC 연동 · Docker 배포 파이프라인

*(GitHub에 릴리스가 없었던 v26.07.3 이후 누적분을 하나의 버전으로 묶음)*

## 시간대(Timezone) 통합 관리

- **예약이 장치 현지 시간 기준으로 저장·표시**: 스케줄 벽시계 입력("06:00" 등)을 장치가 위치한 현지 시간대로 해석해 UTC로 저장하고, 조회 시 같은 순간을 다시 장치 현지 시각으로 재표시. AI 경로(일정 생성 도구)와 사람이 쓰는 수동 작업 생성/편집 모달 양쪽에 동일한 "장치-앵커" 규칙을 적용해, 편집할 때마다 시각이 몇 시간씩 밀리던 버그를 제거.
- **반복(cron) 예약도 장치 시간대·서머타임(DST) 추종**: 반복 트리거가 시스템 시간대 기준 고정 UTC 시각으로 굳어져 있어 서머타임 전환 시 실제 발화 시각이 밀렸던 것을, 장치의 앵커 시간대를 그대로 전달하도록 수정해 DST 전환도 자동 반영.
- **신규 작업 폼에 이중시계 실시간 표시**: 예약 대상과 시각을 바꾸면 같은 순간을 장치 현지 / 내 시각 / UTC 세 줄로 즉시 표시(대상과 내 시간대가 같으면 '내 시각' 줄은 생략).
- **스케줄러 달력 축을 보는 사람의 시간대로 고정**: 달력 시간 축은 뷰어(브라우저) 시간대로 표시하고 하단에 축 시간대 라벨을 붙임. 이벤트 클릭/툴팁은 장치 현지 시각+시간대 라벨을 별도로 표시.
- **개인 표시 시간대 설정 추가**: 상단 User Settings 모달에 Timezone 필드 신설. 설정하면 화면 표시가 개인 시간대를 우선 따르고, 비워두면 시스템 시간대로 폴백(표시 전용, 장치 예약 해석·제어 로직에는 영향 없음).
- **노트 시각을 대상 위치의 현지 시간으로 표시**: 노트가 달린 장치/구역/시설의 시간대로 시각을 변환해 표시, `/notes/target/<id>` 응답에도 위치 시간대(date_tz) 필드 추가.
- **구역/시설의 시간대 자동 상속**: 좌표나 명시 시간대가 없는 장치는 구역 → 시설 → 좌표 순으로 상위 시간대를 자동 상속. 지도에서 도형을 저장할 때마다 포장 → 구역 → 장치 순으로 재계산해 전파하되, 사용자가 직접 지정한 시간대는 덮어쓰지 않음.
- **위성 이미지(GIS) '오늘/어제' 날짜를 장치 위치 기준으로 계산**: 기존 UTC 기준 판단으로 UTC+9 지역에서 위성 타일이 하루 늦게 표시되던 문제 수정.
- **다음 발화 시각 계산의 DST 오차 수정**: 미래 발화 시각을 '지금' 시점의 고정 UTC 오프셋으로 계산해 서머타임 경계를 넘는 예약에서 최대 1시간 어긋나던 버그 수정.
- 관련 매뉴얼: System 섹션에 시간 처리(시간대 계층 구조·예약 흐름·표시 규칙) 설명 문서 추가.

## 스케줄러

- **수동 작업 생성 대상 목록을 전체 실장치로 확장**: AI 전용으로 제한돼 있던 대상 목록 대신, 실제 존재하는 전체 출력/PID/함수/구역 목록에서 고를 수 있도록 개편.
- **새 작업 모달의 숨김 옵션 행 순간 노출 버그 수정.**
- **달력(타임라인) 이벤트 시각 오해석 버그 수정**: 서버가 시간대 정보 없는 UTC 시각을 그대로 내려보내 브라우저가 로컬 시각으로 잘못 해석하던 문제 수정.

## 캘린더 연동

- **Google Calendar 양방향 동기화**: 사용자별 OAuth2 인증(암호화된 refresh token 저장)으로 AoT 일정↔Google Calendar 양방향 동기화. AI/사용자/장치 3개 카테고리로 분리된 캘린더에 구조화된 Key:Value 이벤트 포맷(다국어+유의어 파싱)을 사용해 증분 동기화·충돌 해결.
- **캘린더 대시보드 위젯**: 월/주/목록 반응형 뷰, 편집 모달 내 위치 지도 미리보기, 인라인 편집·드래그 재조정, 스케줄러로 딥링크.
- **캘린더 기반 장치 제어**: 일정에서 출력 on/off/open/close·값/PWM, PID setpoint/게인, 함수 트리거, 입력·컨트롤러 활성화/비활성화를 예약 가능(전부 승인 대기 상태로 생성되는 승인 게이팅 방식).
- **캘린더 선택 피커 + 도메인 중립화**: 위젯 툴바 '캘린더' 버튼으로 표시할 캘린더를 토글, AoT 소스(AI/사용자/장치)와 사용자의 Google 캘린더를 함께 선택 가능. 선택 상태는 위젯별로 저장, 사용자 노출 문구에서 '농장' 전제를 제거해 도메인 중립화.
- **Google Drive 지식 소스 + Google 계정 가입 연동.**

## 로그인 및 공개 페이지

- **공개 랜딩·법적 페이지 신설**: 로그인 없이 접근 가능한 랜딩 페이지를 신규 인덱스로, 개인정보처리방침·이용약관 페이지 추가(Google OAuth 검증용 익명 접근), robots.txt 허용 처리.
- **로그인 버튼 정리**: Google 로그인 버튼을 공식 브랜드 가이드 스타일로 교체, 아이디/비밀번호 로그인 버튼 문구를 "Sign in with AoT Account"로 분리해 두 버튼의 문구·크기 통일.

## 복합장치(Device) 티어 (신규)

PLC·관수 제어기·양액기처럼 여러 Input/Output이 접속정보 하나를 공유하는 실물 장치를 AoT에서 한 단위로 다루기 위한 신규 계층. 정본 설계문서: [docs/design/device-tier.md](docs/design/device-tier.md).

- **소유(primary) 관계 + 청사진 자동 생성**: `parent_device_id` 컬럼(CustomController 재사용, 새 테이블 없음)으로 "누가 이 Input/Output을 소유하는가"를 표현. 장치 모듈이 청사진(channels/inherit_options)을 선언하면 하위 Input/Output이 자동 생성되고, 접속정보를 한 번 저장하면 하위 전체에 전파(단방향 동기화).
- **활성화 연동(cascade)**: 장치를 켜고 끄면 하위 Input이 함께 활성화/비활성화. Output은 자체 활성화 개념이 없어 대상에서 제외.
- **장치 콘솔 페이지(`/device`)**: gridstack 카드(이름·합성 상태 배지·"측정값 N · 제어 N" 카운트·Activate·톱니바퀴) + 설정 모달(구성/연결/하위장치/노트 4개 섹션). 채널 UI는 읽기 전용으로 전환(타입별 조작 UI는 링크로 연결된 Input/Output 페이지 소관), "설정 →" 바로가기는 제거.
- **참조(secondary) 소속 + 장치 간 계층 + 다대다**: `device_member` 조인 테이블로 "이미 다른 장치 소속인 항목을 이 장치에도 참조로 연결" · "장치가 다른 장치를 하위로 포함"을 지원(순환 참조는 조상 체인 검사로 거부). secondary는 cascade에 관여하지 않음.
- **사용자 정의 복합장치**: 청사진 없는 `device_custom_generic.py` — 드롭다운에서 "Custom (no fixed type)"으로 생성 후 연결 UI로 구성.
- **첫 실물 장치 모듈 2종**: Modbus PLC(레지스터 맵 비가정, 접속정보만 공유) / AoT-C(태양광 12V 릴레이 노드 — ChirpStack MQTT Input + 다운링크 Output, 기존 LoRaWAN Class 스케줄러에 등록).
- **삭제/복제 UI**: Input/Output/Function과 동일한 경로 재사용, 얕은 삭제(하위 Input/Output은 유지, 참조만 해제).
- **Input/Output 카드에 소속 장치 배지 노출**: 클릭 시 장치 콘솔로 딥링크.
- **AI 도구에 장치 소속 관계 노출**: `search_devices`/`get_device_list`가 장치 자신(type='device', member_ids)과 하위 항목의 parent_device_name을 함께 반환 — PLC처럼 읽기/제어가 여러 항목에 흩어진 장치를 AI가 한 번에 파악.
- 그 밖에 버그 수정: 채널 이름 저장 누락, 장치 추가 시 탭 격리 미동작, 데스크톱 드로어(우측 패널) 미표시, 연결 후보 목록을 드롭다운으로 통일, scheduler_id를 빈 값으로 저장해도 슬롯맵이 해제되지 않던 문제 등.

## 보안(Security)

관급(공공조달) 납품 요건 점검을 계기로 인증·감사·의존성 전반을 강화. 사용자 매뉴얼: [docs/Security.md](docs/Security.md).

- **원격관리(Remote Admin) pass-the-hash 제거**: 비밀번호가 URL 쿼리스트링에 평문 노출 + 응답에 bcrypt 해시 반환 + 그 해시만으로 로그인 성립(pass-the-hash)하던 체인을 발급형 토큰(secrets 기반, DB엔 SHA-256 해시만 저장) 방식으로 교체.
- **API 키를 평문 저장→해시 저장(SHA-256)으로 전환**: DB 유출 시 즉시 인증에 쓸 수 있던 문제 해소. 마이그레이션이 기존 평문 키로 해시를 계산해 넣으므로 **기존 발급 키는 그대로 계속 동작**(재설정 불필요), 다만 설정 화면에서 키 값을 다시 조회하는 것은 불가능해짐(발급 시 1회만 노출).
- **계정 잠금 + TOTP 2단계 인증(RFC 6238)**: 로그인 실패 카운터를 세션(쿠키만 지우면 초기화)에서 서버·계정 단위로 이전. TOTP는 표준 라이브러리로 직접 구현(자체 설정 화면은 아직 없음, 관리자 개입으로 활성화).
- **비밀번호 재설정 코드를 예측 가능한 random → secrets로 교체**, bcrypt 비교를 `==` → `hmac.compare_digest`로, 흔한 비밀번호 차단 추가.
- **사용자 행위 감사로그**: 로그인 성공/실패/잠금, 사용자 CRUD, 설정 변경, 출력 수동제어, API 키/원격토큰 발급 등을 기록. 조회(`/audit_log`, 필터) + CSV 반출, 법정 최소 보존기간(1년) 자동 정리.
- **웹 보안 설정 정상화**: "Force HTTPS" 설정이 UI에서만 켜지고 실제 Talisman에는 전달되지 않던 배선 누락 수정, CSRF 토큰을 무기한→12시간+자동갱신(백그라운드 재서명)으로 전환.
- **URL 쿼리스트링 API 키 인증 폐기 예고**: 즉시 제거 대신 사용 시 경고 로그 + 스로틀된 감사 기록을 남겨, 실사용 흔적 근거로 제거 시점을 정할 수 있게 함(헤더 방식은 영향 없음).
- **사용자 수식 eval() 제거**: 단위변환·측정값 재척도·Equation 함수 등이 전부 맨 `eval()`이라 edit_controllers 권한만 있으면 데몬 프로세스에서 임의 파이썬 실행이 가능했던 경로를 AST 화이트리스트 평가기(`aot/utils/safe_eval.py`)로 교체. 부수 효과로, 지금까지 문자열 치환 방식이라 `max(x,5)`처럼 변수 글자가 든 식별자를 쓰는 수식이 아예 동작하지 않던 잠재 버그도 함께 해소.
- **보안 점검 자동화 CI**: 라우트 인증 게이트 전수 점검(공개 라우트는 화이트리스트+사유 명시 강제) + CycloneDX SBOM 생성 + OSV 취약점 스캔을 주 1회 스케줄로 실행.
- **의존성 상향으로 알려진 취약점 20건 → 0건**: click/Flask/Pillow/cryptography/gunicorn 등 18개 상향 + setuptools 취약점 해소(제거된 `pkg_resources` 의존을 `packaging.version`으로 교체).
- **깨진 기본 단위변환식 2건 복구**: K→F(유니코드 마이너스 기호로 인한 SyntaxError), GB→MB(대문자 X로 인한 NameError) — eval 교체 작업 중 실사용 수식 251건 전수 검증으로 발견, 마이그레이션으로 기존 설치 데이터도 복구.

## Modbus TCP(PLC) 연동

설계문서: [docs/design/plc-modbus-integration.md](docs/design/plc-modbus-integration.md) · 매뉴얼: [docs/PLC-Modbus.md](docs/PLC-Modbus.md)

- **공유 클라이언트 계층**: PLC 1대에 여러 Input/Output이 붙는 구성에서 연결·락을 (host, port) 단위로 공유(pymodbus 스레드 비안전성 대응), 링크 헬스는 기존 통신상태 레이어(comm_is_fault)와 연동.
- **Modbus TCP Input**: 코일/디스크리트/홀딩/입력 레지스터를 채널 단위로 폴링, data_type·word_order(32bit 워드 순서, 벤더마다 다름)·scale_factor 지정.
- **Modbus TCP 코일 Output**: 기존 ConfirmableOutputMixin으로 명령 확인, readback 불일치·링크 다운 시 팬텀 ON 방지.
- 검증은 시뮬레이터 기준(실제 PLC의 동시연결 제한·scan cycle 부하·RTU 시리얼은 미검증).

## 대시보드 / 설정 모달

- **설정 모달 우측 드로어 도입 + 라이브 자동저장**: 중앙 팝업 설정 모달을 우측 드로어(본문 밀어내기, 백드롭 제거, 한 번에 1개, 반응형 폭)로 교체하고, 저장 버튼을 폐기해 옵션 변경 즉시 자동저장 + 되돌리기(모달 연 시점 상태로 복원) 버튼으로 전환. 표시형 위젯은 body 프래그먼트 스왑, 차트/인터랙티브 위젯은 멱등 재초기화(인터벌 정리·핸들러 재바인딩)로 반영하고, 지도(AoT_map)는 WebGL 컨텍스트 파괴를 피하기 위해 도형·새로고침 주기·3D 지형만 부분 자동저장으로 즉시 반영. 이후 이 드로어 패턴을 Input·Output·Function(조건/PID/트리거/트리거 시퀀스/커스텀)·GIS 입력·AI 에이전트 설정 모달까지 전부 확장(공용 `aot-widget-drawer.js`가 열림 시 `body.aot-widget-drawer-open` 부여·다른 드로어 자동 닫힘·편집 중인 카드 강조를 처리).
- **위젯 설정 드로어에 위젯 식별 표시 추가**: 같은 종류 위젯이 여러 개인 대시보드에서 어떤 위젯을 열었는지 구분이 안 되던 문제 — 드로어 헤더에 위젯 이름을 타입 앞에 표시(예: "위성 · AoT 지도 위젯 설정")하고, 드로어가 열린 위젯의 대시보드 카드 그림자를 진하게 강조. 다른 위젯 드로어로 바로 전환할 때 이전 드로어의 지연된 hidden 이벤트가 새 강조를 지워버리던 레이스 컨디션도 수정.
- **위젯 설정 모달이 유발하던 채팅창 입력 지연 해소**: 옵션이 많은 위젯 설정 모달을 열 때마다 숨은 select/option 노드(~2600개)가 라이브 DOM에 남아, 브라우저 자동완성이 키 입력마다 전체 폼 필드를 재스캔하며 전역 AI 채팅창 입력 지연까지 유발하던 문제. 모달 본문을 `<template>`로 감싸 최초 오픈 시에만 내용을 주입하도록 전환해 해소.
- **그래프 위젯 설정 모달 현대화**: 시리즈/Y축 옵션의 구 Bootstrap 마크업을 다른 위젯과 동일한 aot-modal 현대화 스타일(섹션 제목+토글, 라벨/컨트롤 정렬)로 전환, 시리즈별 색상·데이터그룹화·타입 등 복합 옵션을 위한 2줄 상세 컴포넌트 신규 도입. 저장 로직은 기존 name/id 스킴을 그대로 유지해 무변경.
- **공지 위젯 카테고리 필터 UI 통일**: 체크박스 목록을 다른 옵션 행과 동일한 토글 스위치로 교체.
- **Entry-UI phase9.6 리스타일**: 공용 Entry-UI 스타일시트 개편 및 스케줄러 엔트리 레이아웃 반영.
- **관리 메뉴 업그레이드 항목 배경색 버그 수정**: 과도하게 넓은 CSS 셀렉터 탓에 업그레이드 가능 여부와 무관하게 강조 배경+고정 텍스트색이 적용되어 배경과 텍스트가 같은 색으로 보이던 문제 수정(업그레이드 가능할 때만 배경 적용, 배경 밝기를 서버에서 판정해 텍스트색 선택). 드롭다운 메뉴 hover 시 굵은 글씨 전환으로 가로폭이 미세하게 흔들리던 문제도 함께 수정.

## 색상 시스템 / 상태 표시

- **채널 배경색 노출 + 미리보기 신설**: 개별 Input/Output 채널 행 배경색이 custom_ui 설정으로 바뀌지 않던 문제 — 실제 소비 토큰(`bg_on`/`bg_off`/`bg_pending`)을 찾아 편집 가능하게 하고, On/Off/Pending/Fault 4가지 채널 상태 미리보기(클릭 시 바로 편집)를 신설.
- **경고(오프라인/응답없음) 색상 필드 추가**: 기존에 하드코딩돼 있던 오프라인 틴트를 `bg_warning`/`tint_warning_bg`/`tint_warning_fg` 필드로 노출.
- **custom_ui 색상 미리보기 3중 버그 수정 + 필드 통합**: 팝오버가 실제 렌더 토큰과 다른 필드를 편집하던 문제 수정, 중복 필드 통합(25→20개), 역할 기준 8개 그룹으로 재배치.
- **색상 토큰 위젯 전수 조사 후 3건 수정**: AoT_PID의 active/inactive/pause/hold 배경색이 토큰과 반대로 매핑돼 있던 버그, 지도 장치 라벨이 존재하지 않는 CSS 변수를 참조해 하드코딩 색으로만 렌더되던 문제, 위젯 카드 배경 완전 투명(테마 미반영) 문제.
- **출력 상태 색상 레이스 컨디션/버그 3건 수정**: 페이지 로딩 시 ON 상태 색이 한 틱 동안 잘못 그려졌다 바뀌는 깜빡임, Actuator Paired(자가구동) 행 배경·Open/Close 버튼 강조가 위임형 액추에이터 로직만 읽어 자가구동 장치에서 항상 고정색으로 보이던 버그.

## 벡터 GIS / 지도 (AoT_map)

- **지도 위젯 설정 간소화 + 장치 미표시 버그 수정**: 기본 옵션을 지도 선택·표시·새로고침 등 6개로 줄이고, 장치 필터·측정 패널·3D·라벨 세부조정·도형·뷰 상태는 접힘형 "고급" 섹션으로 이동. 검증 중 장치 필터를 비워두고 저장하면 `include_all_devices`가 강제로 False로 저장되어 장치가 0개 표시되던 버그를 발견해 함께 수정(기존에 이 상태로 저장된 위젯 데이터를 되돌리는 일회성 복구 스크립트도 추가, 기본 dry-run).
- **장치 필터 의미를 "포함 목록"에서 "제외 목록"으로 재설계 + 렌더 버그 재수정**: 위 수정 이후에도 장치 필터를 비워둔 채 저장하면 장치가 0개 표시되는 사례가 재발 — 서버(`collect_devices()`)가 항상 지도 스코프의 전체 장치를 가져온 뒤, 필터에 선택한 장치만 "숨김" 제외 목록으로 적용하도록 전면 재설계 — 클라이언트 라벨/마커 필터도 화이트리스트 방식에서 제외 방식으로 맞춰 수정. 옵션 문구도 "표시할 장치 선택"에서 "숨길 장치 선택"으로 변경.
- **설정 드로어 실시간 적용 범위 확장**: 장치 필터, 측정값 패널(입력/출력/함수 선택), 활성 오버레이 레이어, 기본 지도, 센서 라벨 스타일(마커 종류·채널 수·소수 자릿수·색상 등 12개 옵션), 입력 갱신 주기까지 저장 없이 옵션 변경 즉시 지도에 반영. 드로어를 열 때마다 서버에 저장된 현재값을 다시 동기화(`GET /get_widget_custom_options`)해, 지도 위 컨트롤로 직접 저장한 값과 열려 있던 폼이 어긋나는 문제를 방지.
- **구역/사이트 팝업에서 출력 장치 예약 켜기(신규)**: 구역 팝업의 장치 목록에서 출력마다 "설정" 버튼이 추가되어, 시작·종료 시각을 지정해 지정 시각까지 켜두거나(`00:00`=수동으로 끌 때까지 계속) 예약 가능. 시작 시각이 미래이면 서버에 저장하지 않고 브라우저에서 대기하므로, 탭을 닫으면 실행되지 않는다는 경고를 표시. 출력별 마지막 종료 시각은 세션 동안 기억.
- **지도 저작권(attribution) 표시 개선**: 도구 열에 저작권(ⓘ) 버튼을 신설해 최초 지도 로드·베이스/오버레이 전환 시 자동으로 표시(사용자 조작 없이도 출처가 보이도록 하는 OSM 준수 요건), 실제 조작(휠/드래그/터치/키보드)에만 자동으로 접힘. 서로 다른 지도 소스가 쓰는 "OpenStreetMap contributors"/"OpenStreetMap" 표기 불일치도 정규화.
- **시설 도형(3D)과 라벨 표시 로직 분리**: 시설 3D 도형 옵션이 꺼져 있으면(기본값) 같은 데이터로 그려지는 시설·센서 라벨까지 함께 안 그려지던 버그 수정 — 라벨은 시설 도형 옵션과 무관하게 독립적으로 표시.
- **도형 카테고리 표시 토글 영속화 버그 수정**: 레이어 메뉴의 카테고리별(부지/구역/시설/장비/장치/직접 그린 도형) 표시 토글이 실제 레이어 생성 여부를 결정하는 값과 다른 키에 저장되어, 새로고침하면 켠 토글이 되돌아가던 문제 수정.
- **범례/독 레이아웃 버그 수정**: 계측값 독의 높이를 담은 CSS 변수가 범례 요소로 상속되지 않는 위치에 설정돼 있어 범례와 독이 겹치던 문제 수정. 컴팩트 범례로 전환하는 기준을 화면 폭이 아닌 위젯 자체 폭(<420px)으로 변경 — 좁은 위젯은 넓은 화면에서도 컴팩트하게, 전체 폭 위젯은 폰에서도 넓게 유지.
- **겹친 라벨/마커 고정(pin) UX**: 마우스 오버로 앞에 나타난 라벨·마커가 클릭해 팝업을 연 뒤에도 계속 보이도록 고정되고, 팝업을 닫으면 해제.
- **도형 라이브 토글 미반영 버그 수정 + 고급 섹션 5분할**: 설정창에서 대지·구역 등 도형을 켜도 새로고침 전엔 반영되지 않던 버그 — 최초 로드 시 꺼져 있던 카테고리는 MapLibre 레이어 자체가 생성되지 않아 라이브 토글이 존재하지 않는 레이어를 조작하려다 조용히 무시됐던 것을, 6개 도형 블록을 온디맨드 생성 함수로 리팩터해 해결. 하나로 뭉쳐 있던 "고급" 접힘 섹션도 장치 필터·측정값 패널·3D 지도·라벨 스타일·도형 5개로 분리해 각각 따로 펼칠 수 있도록 하고, 지도가 이미 자체적으로 저장 중인 뷰 상태(위치·줌·회전·레이어) 7개 옵션은 폼에서 완전히 제거.
- **KMA 날씨 범례 스타일 통일**: KMA Weather GIS 입력만 전용 CSS로 별도 구현돼 다른 기상/위성 레이어(ISRIC, OpenWeather, NASA GIBS 등)와 범례 모양이 다르던 문제 — 공용 범례 마크업을 재사용하도록 통일.
- **지도 미리보기/모달 기본 스타일을 GIS 설정과 정합**: GIS 입력 미리보기·지도 검색 모달의 기본 배경 지도가 하드코딩된 OSM 래스터였던 것을, `/geo/design`에 설정된 벡터 스타일을 그대로 사용하도록 변경. 스타일 로드 실패(API 키 오류 등) 시 빈 지도 대신 데모 스타일로 폴백.
- **지도 디자인 도구 매뉴얼 개정 + 도움말 링크 수정**: geo/design·geo/facility 편집 모드를 실제 5모드(Site/Zone/Facility/Equipment/Device)로 정정하고 신규 하우스 배치 워크플로 예시를 추가. geo/design·geo/facility·scheduler 페이지의 잘못 연결돼 있던 도움말(?) 링크도 실제 매뉴얼 문서로 수정.
- **지도 삭제 캐스케이드 버그 수정**: 지도(GeoMap) 삭제가 ORM cascade에만 의존해 GeoFacility(GeoShape와 shape_uuid로 연결, cascade 미선언)를 놓치고 실패해, 지도·도형은 삭제됐는데 시설 행만 고아로 남는 부분삭제 사고가 발생(실제 사고로 지도 1개를 2주 전 백업에서 복구). 지도/오버레이 삭제 경로 전체를 명시적 벌크 삭제 순서로 재작성. 회귀 테스트 추가.
- **MultiPolygon 도형 편집점 미생성 버그 수정**: 필지 가져오기(지번 조회)로 등록한 도형이 MultiPolygon으로 저장되는데 편집 도구가 Polygon만 처리해, 이런 도형은 편집 모드에서 꼭짓점이 하나도 안 생기던 문제 수정.
- **지도 로드 실패 시 빈 상태를 자동저장하던 버그 수정**: 지도 디자인 로드가 404(삭제됨) 외의 이유로 실패하면(타임아웃·5xx·백엔드 재시작 중 순간적 연결 끊김 등) 전부 "처음 여는 지도"로 오판해 빈 캔버스를 그대로 자동저장해버리던 문제 — 실제로 이 경로로 운영 중인 지도 하나가 빈 상태로 덮어써지는 사고가 발생해 백업에서 복원했다. 404가 아닌 실패는 화면/상태를 건드리지 않고 에러 토스트만 표시하도록 수정, 로드 실패를 이유로 절대 자동저장하지 않는다.

## AoT 위젯

- **시퀀스 위젯 오프라인/점검중 라벨 정렬 정리**: 이름 텍스트 옆에 붙어 들쭉날쭉하던 상태 배지를 이름 컬럼 우측 끝에 고정 정렬.
- **AoT 타이머 기본 동작 변경**: 기본 동작 모드를 사이클→단순으로 변경(드롭다운 순서도 단순 우선). "타이머" 기능을 끄면 실행/휴식 입력값 없이 토글이 단순 On/Off로 동작하도록 수정(이전엔 숨겨진 시간 입력값 때문에 저장이 실패). 오류·검증 메시지를 `alert()` 대신 전역 토스트 알림으로 전환, 위젯 도움말 문구 전면 재작성.
- **시퀀스 위젯에서 드래그로 실행 순서 변경**: 행을 눌러 끌면 순서 변경, 동시작동 그룹은 항상 통째로(그룹이 쪼개지거나 단독 스텝이 그룹 안에 끼는 상태 불가). 저장은 옵션 페이지와 같은 키(`gridstack_y`)를 써 화면 순서와 컨트롤러 실행 순서가 항상 일치.
- **시퀀스 함수, 데몬 재시작 시 진행 중이던 사이클 이어서 재개**: 기존엔 데몬을 재시작할 때마다 사이클이 처음부터 다시 시작해 이미 켜져 있던 출력에 불필요한 OFF→ON이 재발송되고 카운트다운도 리셋되던 문제 — 사이클 시작 시각 등을 저장해두고 재시작 시 주기 이내면 복원, 사용자의 실제 비활성화와는 구분해 처리.

## AI 시스템

- **사이트(포장) 질의가 하위 구역까지 포함하도록 수정**: `GeoShape.parent_id`가 실제 데이터에서는 항상 비어 있고(구역-포장 관계는 폴리곤 포함관계로만 성립), site 이름으로 물으면 하위 zone 정보가 조회 범위에서 빠지던 문제. `parent_id` 우선 + shapely 포함관계 폴백으로 상위·하위를 판정하는 공용 헬퍼(`aot/utils/geo_hierarchy.py`)를 신설해 노트 검색·장치 검색(제어 경로 의존)·센서 상세·설비 용량·공간 계층 트리·지도 스코프 제어 6곳에 일괄 적용.
- **엔티티별 노트 다이제스트 자동 주입**: 각 부지/구역/장치의 초기+최근 노트 요약을 AI 컨텍스트에 자동 주입해, "각 장치 노트 확인" 류 질의를 도구 호출 없이 답하고 상세는 `search_notes`로 드릴다운.
- **노트 조회 정확도 개선**: 장치가 출력 레코드와 지도 도형(마커/폴리곤) 두 가지 정체성을 가지고 있어 한쪽으로만 매칭되며 노트를 놓치던 문제 — 이름을 관련 id 전체로 조회하는 공용 리졸버를 신설해 해결. 검색어에 엔티티 이름이 그대로 들어와도 엔티티로 해석하도록 견고화.
- **MCP 도구 카탈로그 단일화**: MCP 도구 목록을 단일 소스(tool_registry)로 통합해, stdio 서버·mcp_aot 엔진에 남아있던 디스패치 누락과 죽은 경로를 정리.
- **geo/design 설비 조회 도구 3종 추가**: 시설 설계 냉난방 용량·체적·환기·관수 요약(`get_facility_capacity`), 지도에 그린 설비의 구역별 관수 설계 요약(`get_map_equipment`), 위치·간격·반경 등 개별 배관 상세(`get_map_equipment_detail`)를 AI 도구로 신규 등록.
- **장치별 AI 판단 포함 토글**: 입력/출력 옵션에 'Include in AI Judgment' 토글 추가, 켜진 장치만 AI 판단·제어 도구에 노출(기본값 True로 전환해 기존 장치도 기본 노출).
- **MCP 서버 중복 등록 방지**: AI 에이전트 설정에서 Add 버튼을 반복 클릭할 때 동일 이름 MCP 서버 행이 조용히 누적되던 문제 수정.
- **출력장치(밸브/펌프/릴레이) 상태 읽기 도구 `get_output_state` 추가**: 그동안 `set_output_state`(쓰기)만 있어 외부 MCP 클라이언트가 장치를 켜고 끌 수는 있어도 지금 상태를 확인할 방법이 없던 문제 — 실시간 on/off, 지속시간, 마지막 켜진 시각(LoRaWAN처럼 확인이 필요한 장치는 실제 확인 시점 기준)을 함께 반환. 읽기 전용이라 쓰기 도구보다 낮은 권한 문턱 자동 적용.
- **외부 MCP HTTP 서버 활성화 토글**: 지금까지 환경변수로만 켜고 끌 수 있던 MCP HTTP 서버를 Settings > General > AI 서비스 화면에서 토글 가능하게 함 — 컨테이너/서비스 재시작 없이 즉시 반영.

## 입력 (Inputs)

- **SenseCAP OpenAPI 센서 입력 추가**: SenseCAP 장치는 LoRaWAN 조인키(AppEUI/AppKey)를 API로 노출하지 않아 로컬 ChirpStack에 직접 등록할 수 없어, SenseCAP 클라우드 OpenAPI를 직접 폴링하는 방식으로 지원(LNS 우회). 기상대·온습도·토양·CO2·pH 등 19종 Device Type 중 선택하면 해당 장치의 채널만 완전 동기화(슈퍼셋으로 노출 후 껐다 켜는 방식이 아니라, 실제 쓰는 채널만 생성/삭제)해 Function·대시보드 선택 목록에 미사용 채널이 뜨지 않도록 함. "과거 데이터 가져오기" 버튼으로 이력을 주 단위로 백필(채널별 기존 저장 시점 기준 중복/공백 없이).

## 하드웨어 연동

- **ChirpStack 유령 on 상태 복구**: 확인 응답(ACK/status)을 한 번도 받지 못한 장치는 재전송을 모두 소진해도 명령 시점의 낙관적 "on" 상태를 영구 보고하던 문제 — 명령 이전 상태를 함께 보관해, 확인받지 못한 장치는 명령 이전 상태로 되돌리도록 수정.
- **장치 통신상태(comm status) 공용 레이어 신설**: 프로토콜 무관하게 "장치가 실제로 응답하는가"를 데몬이 판정해 UI·알림·AI에 전달(`comm_capable`/`comm_last_success`/`comm_is_fault`/`comm_is_pending`). 폴링형 Input은 측정 신선도로, 리스너형(MQTT/ChirpStack 등)은 드라이버별 하트비트 기준으로 자동 판정. 온라인↔오프라인 전이 시에만 알림 대상이 되도록 중앙 스윕(60초 주기)도 추가했으나, 이후 알림은 사용자가 Function(Conditional/Trigger + Send Email)으로 직접 구성하는 경로만 남기고 **하드코딩된 자동 이메일 발송은 제거**.
- **출력 모듈 `comm_is_fault()`가 상위 클래스의 판정 축을 무시하던 버그 수정**: Kasa 5종·Ecowitt가 자체 `output_setup` 상태만 보고 반환해, 상위 클래스가 관리하는 `is_offline()`/명령별 확인오류 축이 무시되고 있었음 — 두 축을 합성하도록 수정.
- **LoRaWAN Class 스케줄러 밸브 인터락(SC-2) 수정**: ext-HB의 `valves` 필드가 애초에 서버로 내려오지 않아 인터락이 조용히 죽어 있던 문제(필드 자체를 추가) + 밸브 상태 비트만 마스킹해 검사하도록 수정(GPS/전원 비트를 밸브 열림으로 오판하던 문제).
- **LoRaWAN 다운링크 페이싱 1.5초→4초**: Class C RX2가 KR920 기본값(SF12)에서 프레임 하나에 약 1.32초를 점유해, 페이싱 간격이 그보다 살짝만 길면 ACK가 항상 송신 한복판에 묻히는 구조적 문제였음(현장 실측 기준 5초 내 응답률 61%까지 저하) — 4초로 늘려 ACK·상태 응답이 항상 조용한 구간에 들어오도록 수정.
- **ChirpStack 릴레이 상태(0xC0) 확인응답 파싱 추가**: RAK3172-C-E(펌프/릴레이)가 보내는 FPort12 `[0xC0, board, state, ack]` 포맷을 인식하지 못해(밸브 전용 `0xB0` 포맷만 처리) 정상 응답이 확인응답으로 반영되지 않던 문제 수정.

## 환경 제어 (env-coordinator)

- **개구부(측창/천창) 구동 주기 분리 + 안전게이트 즉시반영 수정**: 관측·연산 주기와 실제 구동 주기를 분리하고 `actuation_profile`(반응형/표준/완만/직접설정)로 조절 가능하게 함. 안전게이트 발동·급격한 편차 등은 긴급으로 분류해 구동주기를 우회해 즉시 반영하도록 수정 — 이전엔 안전게이트 강제명령도 일반 이동 게이트를 그대로 통과해 최대 180초까지 지연될 수 있었음. 기본값(Standard 180초)은 기존 동작과 동일해 프로파일을 바꾸지 않은 기존 설치는 영향 없음.

## 시스템 / 보안 / 배포

- **`/function` 조건부 옵션 탭 500 에러 수정**: 도움말 링크를 로케일 인식 함수로 일괄 전환할 때 한 템플릿만 번역 문자열 안에 링크가 있어 `{{ }}` 중첩으로 `TemplateSyntaxError`가 나던 문제 수정.
- **Docker 컨테이너 인앱 업그레이드/백업 게이팅 + DB 경로 정본화**: 도커는 베어메탈 전용 인앱 업그레이드/백업 스크립트를 실행하면 부분 실패로 시스템이 훼손될 위험이 있어, 컨테이너 감지 시 업그레이드는 안내만 표시하고 POST를 차단, 백업은 생성/복원을 차단(호스트 git pull·볼륨 스냅샷으로 안내). DB 삭제 경로의 하드코딩된 유령 경로도 실제 설정값 기준으로 교정.
- **Docker 이미지 발행 파이프라인 구축**: 버전 태그 push 시 GitHub Actions로 멀티아치(amd64+arm64) 이미지를 GHCR에 발행, 배포 compose를 이미지 기반(소스 bind-mount 제거)으로 전환해 `docker compose pull && up -d`로 업그레이드 가능(기동 시 alembic 자동 마이그레이션, 볼륨 데이터 보존).
- **관리자 페이지 Docker 네이티브 백업/복원**: 컨테이너에서 막혀 있던 백업 기능을 DB+업로드 디렉터리 직접 스냅샷/복원으로 활성화(호스트 접근 불필요), 복원 시 사전 안전백업 최근 3개 보관. 설정/정보/백업 페이지를 공용 스타일로 통일, 노트 첨부파일 하위폴더 상대경로 보존 버그도 함께 수정.
- **Alembic 마이그레이션 체인 리베이스라인**: 최초 리비전부터 115개 스텝을 단일 베이스라인으로 대체(신규 설치는 영향 없음, 기존 설치는 업그레이드 시 재스탬프 필요).
- **"로그인 유지"(90일) 쿠키 보안 옵션 명시화**: flask-login 기본값(`SECURE=None`, `SAMESITE=None`)에 암묵적으로 의존하던 것을 세션 쿠키와 동일하게 명시(`REMEMBER_COOKIE_SECURE/HTTPONLY/SAMESITE`), 브라우저의 더 엄격한 기본 정책으로 로그인 유지가 조용히 풀리는 것을 방지.
- **`/geo/settings` 저장 동시성(lost-update) 버그 수정**: 짧은 간격으로 두 설정(예: 부지 색상 → 구역 색상)을 저장하면 나중 저장이 먼저 저장분을 덮어써 하나가 유실되던 문제 — 저장 전체를 락으로 감싸고, 락 안에서 최신 행을 다시 조회한 뒤 병합하도록 수정.
- **상태 배경색 팔레트 버그 수정**: `--aot-bg-pause`/`--aot-bg-hold`가 `--aot-bg-active`와 동일한 색상으로 복사돼 있어, 일시정지/보류 상태가 켜짐 상태와 시각적으로 구분되지 않던 문제 수정.
- **로컬 개발 docker-compose의 MCP 서버 바인딩을 Tailscale IP로 변경**: 외부 리버스프록시(NPM) 경유 접속을 위해 `127.0.0.1` 대신 이 호스트의 Tailscale IP에만 바인딩(같은 tailnet 밖으로는 여전히 노출되지 않음).
- **업그레이드/의존성 로그 타임스탬프의 moreutils(`ts`) 의존 완전 제거**: 앞서(v26.07.3) 도입한 `command -v ts && ts || cat` 폴백도 `ts` 미설치 환경에서는 여전히 타임스탬프 없이 찍혔다 — `ts` 없이 항상 타임스탬프를 남기는 순수 bash 루프로 17곳 전량 교체.

## 문서

- **AoT_map 위젯 설명서 전면 재작성**: 실제 코드 기준으로 기본값을 정정하고 존재하지 않는 옵션 설명을 제거, 누락됐던 옵션 그룹을 추가.
- **시퀀스 함수·위젯 설명서 개선**: 펌프+밸브 순차 관수·동시 작동(장치 그룹) 예시를 표와 스크린샷으로 추가.
- AoT_graph·AoT_timer 위젯 설명서를 실제 코드 동작 기준으로 재작성(실전 예시·스크린샷 보강, 화면에 효과 없는 옵션 설명 제거).
- **복합장치(Device) 티어 설계 정본 문서** 신규 추가([docs/design/device-tier.md](docs/design/device-tier.md)) — 데이터 모델·청사진 생성·설정 모달 구조와 그 근거를 정리.
- **Modbus PLC 연동 설계 문서 + 사용자 매뉴얼(en/ko/ja)** 신규 추가 — 벤더 문서와의 주소 체계 차이(1-based vs 0-based), 32bit 워드 순서, 연결 공유의 부수효과 등 실제 연결 시 막히는 지점 위주로 작성.
- **사용자 매뉴얼에 Security 페이지 추가(en/ko/ja)** — TOTP·계정 잠금·API 키 해시화·감사 로그·원격관리 토큰 전환을 문서화, 낡은 메뉴 경로 서술도 함께 정정.
- **환경 제어(env-coordinator) 매뉴얼에 전체 옵션 레퍼런스 + 실제 안전게이트 반영** — 예시 위주였던 절을 실제 코드 기준 18개 섹션·약 50개 옵션 전체 표로 교체, 문서에만 있던 존재하지 않는 안전게이트 항목도 제거.

---

# v26.07.3 (2026-07-20) — 인앱 GitHub 업그레이드 정상화 · config 정본 정합

## 시스템 / 배포

- **인앱 GitHub 업그레이드 실패 3종 근본원인 수정**: (1) `ts`(moreutils) 미설치 시 실행 파이프가 SIGPIPE로 붕괴 → 업그레이드 미실행·로그 0바이트. `ts` 의존 17곳을 `command -v ts && ts || cat` 폴백으로 교체(타임스탬프만 잃고 로그·실행 보존). (2) `upgrade_install.sh`가 구 레이아웃 루트 `databases/`를 참조해 GIS `aot/databases/`와 어긋나 DB 복사에서 중단 → 경로 정합(+wipe 존재검사 `-d`→`-f`). (3) config가 매 import마다 stderr로 찍던 디버그가 `URL=$(... 2>&1)` 캡처에 섞여 `wget` URL을 파괴 → 스크립트 캡처 `2>/dev/null`·config 디버그 `AOT_CONFIG_DEBUG` 게이팅·regex raw-string으로 `SyntaxWarning` 제거.
- **config 패키지 이전 잔재 정합**: `config.py`를 `aot/config/__init__.py` 패키지로 병합한 뒤 남은 '존재하지 않는 `aot/config.py`' 유령 참조 2곳 교정 — `release_helper.py`(버전 자동범프가 `FileNotFoundError`로 사망하던 것, 릴리스 태그 불일치의 원인)·`settings_diagnostic_upgrade_master`(마스터 업그레이드 토글 무동작). 무력했던 2D→GIS 호환성 가드 경로도 교정.

---

# v26.07.2 (2026-07-19) — 에이전트 루프 아키텍처 전환

## AI 시스템

- **에이전트 루프 아키텍처**: 라우터 팬아웃(Router→Planner→Executor→Synthesizer) 대신 단일 상태보존 루프(전체 도구 카탈로그 + `ask_user` 확인 + 모델 불가지)로 전환, 기본 경로로 승격(레거시는 플래그로 즉시 롤백 가능하도록 보존)
- **레거시/UOC 서브시스템 정리**: 고아 상태였던 `ai/knowledge`·`ai/orchestration`·`ai/ui`·`ai/validation` 서브시스템 및 4종 shim 모듈, `utils/sse_manager.py` 삭제
- **지식베이스 강화**: 출처 추적(provenance), chunk 재사용 카운트·플래그 사유 필드 추가, 지식 주입 기본 활성화, 승격/보관(promotion/shelve) 서비스 신설
- **스마트팜코리아 외부 소스 연동**: 시설/노지/축산 3개 데이터셋을 AI 컨텍스트 소스로 등록·조회
- AI 라이브러리/채팅 UI 정비, 지도위젯의 구형 'AI 조언 표시' 옵션 제거(신규 스코프별 조언 칩으로 대체됨)

## 벡터 GIS / 지도

- **지도 LCP 지연 해소**: `/runtime` 센서 스냅샷을 논블로킹 캐시로 전환, 액추에이터 요약(LCP 요소)이 라이브 InfluxDB 조회를 기다리지 않도록 수정

## 노트

- 노트 드로어/입력 컴포넌트 UI 개선, 공용 드로어 CSS 컴포넌트 분리

## i18n

- 카메라 장치 카탈로그·GIS 입력 모듈·시설 용량산정 라벨의 한국어 하드코딩 문자열을 `gettext` 래핑 및 영문 기본값으로 전환, 전체 언어 번역 카탈로그 재추출

## DB 마이그레이션 (HEAD: `p5_52_agent_loop_default_on`)

`p5_46` → `p5_47`(knowledge provenance) → `p5_48`(flagged_reason) → `p5_49`(reuse_count) → `p5_50`(지식주입 기본on) → `p5_51`(agent_loop 카나리 플래그) → `p5_52`(agent_loop 기본전환)

---

# v26.07.1 (2026-07-18) — 레거시 2D→GIS 업그레이드 가드

- **업그레이드 호환성 가드**: 레거시 2D(지도) 에디션에서 GIS 에디션으로의 in-place 업그레이드를 차단(`upgrade_install.sh`). 두 에디션은 DB 스키마(alembic 계보)가 비호환이라 in-place 업그레이드 시 데이터가 손상됨. 2D 설치(config 단일모듈) 감지 시 아무것도 변경하기 전에 중단하고 신규 설치를 안내. → 레거시 사용자는 업그레이드가 GIS 최신(major 26)으로 연결되던 위험에서 보호됨.
- `upgrade-master` 도움말 URL을 `AoT-inc/AoT`로 정정(Mycodo 잔재 제거).

---

# v26.07.0 (2026-07-18) — 벡터 지도·3D·AI 공개 릴리스

벡터 지도(GIS)·3D 시설 관리·AI가 포함된 공개 버전입니다. 이전 2D 지도 기반 버전(v26.0.x)은 `legacy-2d` 브랜치에 보존되어 있으며, 본 버전과 DB 스키마가 호환되지 않습니다(구버전에서 업그레이드 불가, 신규 설치 필요).

## 벡터 GIS / 지도

- **드론·항공사진 이미지 오버레이** + 대형 이미지 XYZ 타일화(줌 대응)
- **측정값 패널**: 제어상태·정렬·풍향 표시, 문자 비율 조절 핸들, 패널 테마 전역 적용
- **액추에이터 오버레이 막대 렌더** + 소형 위젯 compact 모드
- **시설 라벨**: 충돌 시 중앙 기준 좌우 자동 배열, 단동 시설 구역 칩(bay/이름)
- **zone/facility 모달**: zone 정보 모달, 장치 작동 이력 차트, facility 센서 모달 통일
- **사이트 목록 모달**: 아코디언 + zone 드롭다운 + 사용자 정의 순서
- 레이어별 저작권 표시, 라벨 마스터 스위치, MapLibre 로컬 서빙 옵션
- 3D 위젯: renderMode 초기빌드·performance 모드·중복 렌더 수정
- 성능: MapTiler preconnect, 지도 위젯 공통 fetch 공유

## 환경 제어

- **창호 position-form PI 재설계**: 릴레이 진동 제거, 장치 실측 위치 기준 동기화
- **개도 규약 통일**(100%=열림): 커튼·차광막 이중반전 제거
- **안전 게이트**: 강풍/강우 시 내부 스크린(차광막·보온커튼) 강제 해제, 스크린 idle 기본값 걷힘
- **팬 모델 현실화**: 덕트 없는 벽면 배기팬은 창 열림 시 무력, 환기팬 구배의존·유효도 게이트
- **VPD 직접제어 전환** + 입력별 자동 VPD 계산 서비스
- PID 스케줄, 주간 스케줄, 환경 요약 스냅샷, 시설 사진 업로드

## LoRaWAN / ChirpStack

- **사이트 단위 Class A/C 스케줄러** + 공통 ChirpStack REST 헬퍼
- **밸브 명령 신뢰성**: 업링크 확인, 앱레벨 재전송, 전역 페이싱
- **스케줄러 강화**: reconcile·밸브 interlock·배터리 게이트·우선순위
- HB 전류 디코딩(7바이트), valve_active 전류 기반 판정, RSSI/SNR 측정타입 정정
- ChirpStack API 키를 API Keys 저장소로 통합, tenant/application 필터

## Function / 시퀀스

- **시퀀스 장치 그룹(동시작동)**: 그룹 묶기/해제, 위젯 행 접기
- **요일별 작동**: 요일 선택, 요일별 그룹·작동시간, 리더 자동상속
- **통합 모달 시간휠 UI** + 위젯 목록 재설계·전역 툴팁
- 단일 장치 시간축 누적/지점 합산 function, sum_accumulate 즉시 측정 버튼
- 액션 순서 저장, 소스 디바이스 타임존 기준 집계, execute_now 기준점 리셋 등 다수 수정

## UI / UX

- **색상 시스템**: 테마 프리셋(측정 밴드·차트 시리즈 포함), z-index 토큰화
- **브랜딩**: 네비바 브랜드 이미지(SVG·GIF·WebP), 데몬/앱 상태 표시 개선
- **그래프**: 이전 기간 on-demand 로드, 기간버튼 최신시점 앵커 수정, 모바일 툴팁
- 모달 모던화(모바일 중앙 카드), 위젯 설정 모달에서 탭 이동, 장치 탭 이동
- i18n: GIS 오버레이 범례 등 다국어 번역 정비, 하드코딩 영어 메시지 gettext 래핑

## AI 시스템

- **v3.1 티어 아키텍처**: Tier0 사전분류 → Router → Planner → Supervisor → Synthesizer 파이프라인 재편, 목표지향 연속 실행 루프
- **AI 엔티티 관리**: Input / Output / Function / GIS 배치 / AI Agent 5종을 AI가 생성·편집·삭제, 변이 작업은 사용자 승인 게이팅
- **지식 다이제스트 파이프라인**: 멀티사이트 facility 스코핑, 환경제어 조언 연동, 시스템 지식 레이어
- **지도 연동**: 지도 뷰포트 스코프 장치 제어, 배치 승인, 위치(구역) 기반 장치 해석, 스코프별 AI 조언 칩·Q&A
- **서버측 페이지 컨텍스트 조립**: 대시보드 상태를 서버에서 구성해 DOM 스크래핑 대체
- **다국어**: 사용자 대면 문자열·분류 규칙 언어무관화
- 안정화: MCP 기동·OOM 크래시루프·장치목록 조회·팬텀 승인버튼·대화맥락 수정

## 시스템 / 배포

- 환경 적응형 gunicorn 스레드, 서비스 재시작·리로드 일원화
- Docker: aot 사용자/그룹 생성, InfluxDB 포트 127.0.0.1 전용 바인딩
- 대시보드 `/last`·`/past` 요청을 배치 API로 통합해 요청 폭증 해소
- JS 빌드 오케스트레이터(rollup/esbuild) — geo/notes 번들 통합 관리

## DB 마이그레이션 (HEAD: `p5_46_input_auto_vpd_toggle`)

26.06에서 alembic 계보를 재베이스라인하여 구 8.16.x 계열과 분리했습니다. v26.0.x(legacy-2d) DB는 본 버전으로 마이그레이션할 수 없습니다.

---

# v26.05.0 (2026-05-18) — 최초 공식 릴리스

## AI 시스템

- **MCP 서버** (`aot/mcp_server/`): FastMCP 기반, stdio/HTTP SSE 모드 지원
  - `Observe` 도구: 시설 상태·센서 이력·액추에이터 명령값 조회
  - `Diagnose` 도구: 센서 이상 탐지·환경 제어 성능 분석
  - `Control` 도구: VPD 목표값·메서드 제어점 변경, 수동 잠금 (사용자 승인 필요)
- **MCPAuditLog / MCPConfirmation**: 도구 호출 이력 및 승인 큐
- **AIAgent `allowed_tools`**: 에이전트별 MCP 도구 접근 범위 제한

## 벡터 GIS

- **GeoFacility fittings**: 창호·도어·커튼 등 시설 구성 요소 등록 및 GeoShape 연결
- **시설 센서 바인딩** (`facility_sensors.py`): 실내외 센서를 역할별로 시설에 연결, 가중 평균 산출
- **시설 풍향 분석** (`facility_wind.py`): 외부 풍향 데이터 처리 및 개구부 방위각 비교
- **GeoFacility timezone** 필드 추가

## 통합 환경 제어

- **Function cumulative state**: DLI·GDD 일별 누적 및 보상 제어 (`FunctionCumulativeState`)
- **Function crop preset**: 작물 광합성 파라미터 DB 저장 (`FunctionCropPreset`)
- **lighting 액추에이터**: `ACTUATOR_KINDS` 정식 등록, R2 경로(보광 등록 시 활성)
- **풍향 차등 SafetyPreGate**: windward 60° arc 내 개구부만 강제 폐쇄, leeward 환기 유지
- **EffectFn 면적·단열 가중**: 개구부 면적·u_effective 반영 효과 산출
- **GIS 기반 profile builder**: GeoShape per-device 우선 조회, fallback 균등 분할 유지
- **env_control 모듈**: `authority`, `calibration`, `cumulative_tracker`, `forecast_feedforward`, `photosynthesis`, `group_expander`, `ext_context_fallback`
- **작물 프리셋**: 상추·파프리카·고추·딸기·토마토 VPD 프리셋 기본 제공

## DB 마이그레이션 (HEAD: `p5_6_geo_facility_fittings`)

`p2_5` → `p3_5` → `p4_3` → `p4_4` → `p5_1` → `p5_5` → `p5_3` → `p5_6`
