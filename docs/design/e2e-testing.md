# E2E(종단) 검사

브라우저로 실제 화면을 여는 유일한 검사 층이다. 나머지 CI 워크플로는 전부
정적 가드이고, 단위 스위트도 브라우저를 켜지 않는다. 그래서 아래 같은 사고는
여기서만 걸린다.

- CSS 가 통째로 안 실려 화면이 무너지는 것(`@layer` 순서, 주석 오타, 링크 누락)
- 인라인 JS 가 깨져 그리드스택 같은 것이 통째로 죽는 것
- 저장은 됐다는데 새로고침하면 사라지는 것
- 카드가 그려지긴 하는데 누를 것이 없는 것

## 돌리는 법

```bash
aot/scripts/run_e2e.sh          # 기동 → 시드 → L0 → L1
aot/scripts/run_e2e.sh l0       # 라우트 스모크만 (~15초)
aot/scripts/run_e2e.sh l1       # 페이지 부팅 검사만 (~2분 30초)
aot/scripts/run_e2e.sh l1 /geo/design        # 한 화면만
AOT_E2E_HEADED=1 aot/scripts/run_e2e.sh l1 /dashboard   # 브라우저를 보면서
```

준비물은 `pip install playwright && playwright install chromium` 뿐이다.

`pytest aot/tests` 는 **지금까지와 똑같다**. `AOT_E2E_BASE_URL` 이 없으면
`aot/tests/e2e` 는 수집조차 되지 않으므로, 브라우저도 서버도 필요 없고 수집
시간도 그대로다. 환경변수를 주는 것이 곧 "E2E 를 돌리겠다" 는 의사 표시다.

## 환경 — 개발 스택과 완전히 분리한다

`docker/docker-compose.e2e.yml` 이 프로젝트명 `aot-e2e` 로 8085 에 뜬다.
개발 스택(`aot_local`, 8084)과 볼륨·포트가 전부 갈린다. 라이브 compose 가
bind mount 로 쓰는 것(`../influxdb_data`, `../logs`)은 여기서 전부 named
volume 이다 — **bind mount 는 프로젝트명으로 갈라지지 않기 때문이다.**
공유하는 것은 코드(`../:/app`)뿐이고, 그것이 목적이다(지금 브랜치를 시험한다).

데몬은 `--profile control` 뒤에 있다. L0~L2 는 데몬 없이 판정되고 L3 만
데몬을 쓴다 — 다만 **CI 는 항상 데몬까지 띄운다.** 건너뛴 검사는 없는 검사와
같기 때문이다(데몬이 없으면 L3 는 skip 된다).

## 픽스처

`aot/tests/e2e/seed.py` 가 컨테이너 안에서 돈다. `AOT_E2E=1` 이 아니면
아무것도 하지 않고 종료한다 — 개발/운영 DB 에 픽스처를 쓰는 사고를 막는
안전핀이다. 멱등이라 테스트 사이에 다시 불러도 된다.

이름과 값은 `aot/tests/e2e/fixtures.py` 한 곳에만 적는다. 시드(컨테이너 안)와
테스트(호스트)가 같은 파일을 읽어야 둘이 조용히 어긋나지 않는다.

## 층

| 층 | 파일 | 보는 것 | 시간 |
|---|---|---|---|
| L0 | `test_route_smoke.py` | GET 라우트 전수 — 5xx·렌더 실패 없음, 익명은 전부 차단 | ~15초 |
| L1 | `test_page_boot.py` | 페이지마다 **부팅됐는가** | ~2분 30초 |
| L2 | `test_journey_*.py` | 사람이 하는 일 — 저장하고 되읽기까지 | 시나리오마다 |
| L3 | `test_control_loop.py` | 누른 것이 **데몬까지 갔는가**, 실패가 정직하게 돌아오는가 | ~1분 |

### L0 — 대상이 자동으로 자란다

돌고 있는 앱의 `app.url_map` 을 덤프해서 쓴다(`dump_routes.py`). 소스를 grep
하지 않으므로 조건부로 등록되는 것까지 포함하고, **새 페이지를 만들면 다음
실행부터 자동으로 검사 대상이 된다.** 아무도 테스트를 쓰지 않아도 렌더가
터지는 화면은 여기서 걸린다.

익명 검사는 `check_route_auth.py` 와 짝이다 — 그쪽은 데코레이터를 정적으로
보고, 이쪽은 **실제 응답**을 본다. 데코레이터가 붙어 있는데도 응답이 새는
경우가 실제로 있었다.

### L1 — 부팅 판정 항목

페이지마다 아래를 전부 본다.

1. JS 콘솔 오류 0건
2. 4xx/5xx 응답 0건
3. **규칙이 있는데 하나도 적용되지 않은 스타일시트 0개**
4. 앵커 요소가 그려져 있고 높이가 0 이 아님
5. 본문 높이 > 0, 가로로 넘치지 않음

3번이 `@layer` 순서 사고와 주석 오타 사고를 정확히 잡는 조건이다. 단,
**비어 있는 것이 정상인 파일**이 있으므로(예: `custom-light.css` 는 "라이트
기본값과 다른 것만" 담는 규칙이라 지금은 주석뿐이다) 파일을 실제로 받아
규칙 블록이 있는지 확인한 뒤에만 사고로 센다.

판정이 "페이지가 부팅됐는가" 이므로 기능이 바뀌어도 잘 깨지지 않는다. 대신
대상이 넓다 — **손대지 않은 화면이 죽는 것**을 잡는 것이 목적이다.

대상 선정은 `pages.py` 의 `NOT_A_PAGE` 로 거른다. 새 라우트가 페이지가
아니면 여기에 추가한다. 귀찮아 보이지만, 새 표면이 생겼다는 사실을 한 번은
보게 만드는 것이 목적이다.

무시할 콘솔 오류·실패 응답은 **사유와 함께** 목록에 등록한다
(`check_route_auth.py` 의 `PUBLIC_ROUTES` 와 같은 방식).

### L2 — 지금 있는 여정

| 파일 | 무엇을 고정하나 |
|---|---|
| `test_journey_login.py` | 익명은 튕기고, 잘못된 로그인은 **막혔다고 말하며**, 로그아웃하면 다시 막힌다 |
| `test_journey_dashboard.py` | 위젯을 끌어다 놓으면 저장되고 새로고침해도 그 자리에 있다 |
| `test_journey_map_shapes.py` | 저장된 도형이 지도에 남아 있다(전량 유실 감시) |
| `test_journey_map_draw.py` | 지도에 **그린** 도형이 저장되고, 하나를 지우면 **그 하나만** 지워진다 |
| `test_journey_map_view.py` | 전체화면을 오가도 지도가 사라지지 않는다 |
| `test_journey_output.py` | 출력 카드에 누를 것이 남아 있다 |
| `test_journey_journal.py` | 일지를 만들면 완성되고, 주소로 다시 열린다 |
| `test_journey_modal_save.py` | 설정 모달에서 고친 값이 새로고침 뒤에도 그대로다 |
| `test_journey_input_crud.py` | 입력을 만들고·고치고·지우는 한 바퀴 |
| `test_journey_function_crud.py` | 함수로 같은 한 바퀴(화면마다 구조가 다르다) |
| `test_journey_permissions.py` | 권한 없는 사용자가 못 하는 것과 **할 수 있어야 하는 것** |
| `test_journey_notice.py` | 공지가 목록에도 대시보드 위젯에도 뜬다 |
| `test_journey_user_settings.py` | 언어·화면 배율이 전 페이지에 먹는다 |
| `test_journey_theme_color.py` | 색 정본이 `/custom.css` 와 다른 페이지까지 간다 |
| `test_journey_plots.py` | 구획 목록이 사실(경과 일수·면적)과 함께 보인다 |
| `test_journey_scheduler.py` | 예약한 일이 달력 자료에도 화면에도 있다 |
| `test_journey_facility.py` | 시설 저장이 조건을 지키고 막을 때 알려 주며, **지도에 배치해 만든** 시설의 외곽이 입력한 치수이고, 지우면 외곽까지 사라진다 |
| `test_journey_sequence_order.py` | 시퀀스 단계를 끌어 옮기면 저장되고, 다시 열어도 그 순서다 |
| `test_journey_graph_period.py` | 그래프에 점이 있고, 기간 버튼을 누르면 축이 **그 기간**만큼이다 |
| `test_journey_ai_approval.py` | AI 제어 요청은 결정 전엔 안 돌고, **결정할 자격이 있는 사람만** 결정하며, 승인하면 적힌 그대로 돈다 |
| `test_journey_camera.py` | URL 카메라로 **장치 없이** 추가·설정·촬영·마지막 이미지·삭제, 게스트는 못 보고 못 건드린다 |
| `test_journey_energy_usage.py` | 에너지 두 화면의 숫자가 검산과 맞는다(시간 × 전압 × 전류), 모니터의 추가는 500 이 아니라 거절 |
| `test_journey_logs_audit.py` | 로그뷰가 읽은 줄을 그리고 거르기가 실제로 거르며, 로그인 실패가 **접속 주소와 함께** 감사 로그에 남는다 |
| `test_journey_upgrade_badge.py` | 업그레이드가 있으면 메뉴 항목이 강조되고 호버해도 **읽힌다**(명암비) |
| `test_journey_geo_layer.py` | 지도 레이어 추가·이름 변경·켜기(지도 편집 화면이 받는다)·삭제 |
| `test_journey_method.py` | 메소드 구간을 쌓으면 `/method-data` 곡선이 그 구간과 같다 |
| `test_journey_mcp_servers.py` | MCP 서버 등록 → 환경변수가 그대로 돌아옴 → 연결 시험이 도구 목록을 받음 → 삭제, 역할별로 보이는 것 |
| `test_journey_export.py` | 측정값 CSV, 설정 백업 zip 이 **다시 가져오기의 이름 검사를 통과**한다 |
| `test_journey_field_values_stay.py` | 입력칸을 벗어나도 넣은 값이 그대로다(시간 표기 도우미가 엉뚱한 칸을 망가뜨리던 회귀) |

### L2 — 저장은 되읽어야 끝난다

모든 쓰기 시나리오는 `저장 → 새로고침 → 되읽기`까지 간다. 중간만 보면
"저장된 것처럼" 보이는 상태와 진짜 저장된 상태를 구분할 수 없다.

버튼 문구를 셀렉터로 박지 않는다. 문구를 박으면 UI 를 손볼 때마다 거짓
경보가 나고, 정작 겨냥한 사고("조작 수단이 통째로 사라짐")는 "누를 것이
하나라도 있는가" 로 충분히 걸린다. 버튼을 가려야 할 때는 `processRequest(this,
'output_mod')` 처럼 **동작**으로 가린다 — 언어를 바꿔도 그대로다.

#### 셀렉트는 드롭다운을 열어서 고른다

`journeys.choose_option()` 을 쓴다. 이 앱의 셀렉트는 bootstrap-select 로
감싸져 있고, 화면 코드는 대개 `changed.bs.select` 만 듣는다(`journal-page.js`
의 `_onSelect`). `<select>` 값을 바꿔 네이티브 `change` 를 쏘는 방식으로는
**아무 일도 일어나지 않는다** — 값은 바뀌었는데 화면은 고른 줄 모른다.
그리고 열린 목록은 래퍼가 아니라 **body 바로 아래**에 붙는다
(`data-container="body"`).

#### 비동기로 만들어지는 것은 완성을 기다린다

일지가 그렇다. 생성 요청이 200 을 준 직후의 화면은 "생성하고 있습니다" 다.
그 상태를 통과로 세면 **영영 완성되지 않는 문서**를 못 잡고, 실패로 세면
거짓 경보가 난다. 완성 조건을 적어 기다린 뒤에 본다.

#### 자기가 바꾼 것은 되돌린다

여정이 픽스처를 고쳤으면 끝에서 되돌린다. 다음 여정이 같은 상태를 전제하기
때문이다. 되돌리기도 **단언한다** — 조용히 실패하면 그 뒤 검사들이 엉뚱한
전제 위에서 돈다. 시퀀스 순서처럼 "맨 위를 맨 아래로" 가 회전인 경우, 한 번
더 한다고 제자리가 되지 않는다(단계 수만큼 돌려야 원위치다).

#### 시드 뒤에는 데몬을 다시 읽혀야 한다

데몬은 **기동 시점의 DB** 를 들고 있다. 시드가 장치를 지우고 새로 만들면
uuid 가 바뀌는데, 먼저 떠 있던 데몬은 그것을 모른다 — 제어 폐루프가 "그 출력의
상태가 아예 없음" 으로 깨진다. `run_e2e.sh` 와 CI 는 시드 다음에 데몬을
재시작한다. 손으로 돌릴 때도 순서는 **시드 → 데몬 재시작 → 검사** 다.

### L3 — 데몬이 살아 있는지는 **응답으로만** 판단한다

컨테이너가 `running` 인 것, 로그가 "AoT daemon started" 라고 말하는 것, 그리고
앱이 실제로 데몬과 말이 되는 것은 셋 다 다른 사건이다. 처음엔 로그로 판단했다가
**컨테이너를 멈췄다 켜면 로그가 누적된다**는 것을 놓쳐, 이전 실행의 그 줄을 보고
아직 포트도 안 연 데몬에 제어를 보냈다(2026-09-18 실측). 지금은 "출력 상태가
돌아오는가" 로만 본다.

실패는 **데몬을 잠시 내려** 만든다. 가짜 오류를 주입하는 것보다 현실적이다 —
데몬이 죽어 있는 동안 사람이 버튼을 누르는 일은 실제로 일어난다. 끝나면 되살린다.

#### 지도 편집은 **그리기와 지우기의 저장 방식이 다르다**

이 비대칭을 모르면 삭제 검사가 "지웠다는 화면" 만 보고 통과한다.

| | 언제 서버로 가나 | 어디로 |
|---|---|---|
| 그리기 | 놓는 즉시 | `POST /api/geo/overlays/delta` (한 건) |
| 지우기 | **전역 저장(`#btn-save-global`)을 눌러야** | `POST /api/geo/overlays` (전체) |

지우기는 `_pendingDeletes` 에 쌓이기만 하고 화면에서는 이미 사라진다(되돌릴 수
있게 하려는 설계다). 그래서 삭제 여정은 저장을 누르는 데까지 가야 한다.

삭제 대상이 되는 조건도 둘 있다.
- **활성 모드와 도형 종류가 같아야 한다**(`_isModeAllowedStrict`). `geoDesign
  .activeMode` 가 `site` 면 `site` 도형만 삭제 후보에 오른다.
- **삭제 모드를 켠 뒤에야** 레이어가 로드된다(실측: 켜는 순간
  `featureGroup.layers` 가 2 → 20여 개로 늘었다). 켜기 전 화면 상태로 판단하면
  "지울 것이 없다" 로 보인다.

지워지는 것은 클릭 지점의 **최상단 하나**다(`queryRenderedFeatures` 의 z 순서).
겹쳐 있으면 위에서부터 한 번에 하나씩 — 2026-06 에 전량 삭제를 고치며 세운
규칙이고, 이 여정이 그 재발을 지킨다.

#### 화면마다 같은 것이 다르게 들어 있다

여정을 늘리며 반복해 걸린 것들이다. 한 화면에서 통한 셀렉터를 다른 화면에
그대로 쓰면 **조용히 0건**이 된다.

| 무엇 | 어디에 |
|---|---|
| 장치 이름 | 입력 화면은 **텍스트**, 출력·함수 화면은 **입력 칸의 값** |
| 색 입력 | `name` 없이 **id 로만** (옆에 hex 칸·스와치가 따로 있는 커스텀 컨트롤) |
| 삭제 확인 | 브라우저 `confirm` 이 아니라 앱의 `#aotConfirmModal` |
| 메뉴 항목 | 링크 중심점을 부모 `<li>` 가 덮는다 — **부모를 누른다** |

그리고 카드는 **이름이 아니라 id 로 추적한다.** 같은 이름이 둘 생기는 순간
(이전 실행의 잔재, 중복 생성) 이름 기반 조작은 엉뚱한 것을 고치고 지운다.
id 와 이름은 **한 번에 쌍으로** 읽는다 — 따로 읽어 인덱스로 맞추면 이름이 빈
카드 하나에 어긋난다.

#### 모델이 없어도 승인 게이트는 검사한다

AI 승인 대기는 모델이 무엇이었든 `MCPConfirmation` 한 행이다. 시드가 그 행을
직접 심으면(`_seed_approvals`) 내장 모델이 꺼진 E2E 스택에서도 게이트 —
대기·권한·승인 즉시 실행 — 를 끝까지 태울 수 있다. 검사가 상태를 바꾸므로
쓰임새(승인·거부·게스트 시도)마다 한 행씩 둔다.

#### 시설 편집기는 단계마다 보이는 칸이 다르다

치수(`#length-m`)는 **기본** 단계에, 지도 고르기와 "지도에 배치" 는 **위치·구조**
단계에 있다. 숨은 단계의 칸은 채울 수 없다(보이지 않는 요소). 지도 캔버스도
위치 단계에 들어가야 크기가 생긴다. 배치는 지도 카메라를 가정하지 않고 캔버스
가운데를 누른다 — 카메라는 지도 편집 화면에서 모드를 바꿀 때 그 순간 화면으로
저장되므로, 앞선 여정에 따라 어디든 될 수 있다.

#### 측정값이 안 보이면 저장부터 의심한다

그래프가 비었을 때 화면을 먼저 의심했는데, 원인은 **저장**이었다. 새 도커
설치에서 InfluxDB 버전 감지가 사이드카 대신 `localhost` 에 물어 버전이 빈 값으로
굳었고, 그 뒤 모든 쓰기가 "Unknown Influxdb version" 으로 거부됐다
(`measurement_db._ping`). 이미 버전이 채워진 설치는 영향이 없어 개발 스택에서는
보이지 않았다.

#### 권한 검사보다 CSRF 가 먼저다

쓰기 요청을 게스트로 보내 "403 이어야 한다" 를 보려면 **CSRF 토큰을 실어야**
한다. 앱 전역 CSRF 검사가 블루프린트의 역할 검사보다 먼저 돌아서, 토큰이 없으면
역할과 무관하게 400 이 온다 — 그러면 역할 검사가 없어도 검사가 통과한다.
`conftest.csrf_headers()`·`form_csrf()` 를 쓴다.

#### 같은 이름의 칸이 화면에 여럿이다

메소드 목록에는 메소드마다 `name` 칸과 `method_type` 칸이 있다. 페이지 전체에서
`input[name=name]` 을 고르면 엉뚱한 칸을 채운다 — **폼 안으로 좁혀** 고른다.
공용 셀렉트 드롭인은 원본 `<select>` 를 바꿔 끼우므로 값을 직접 넣어도 폼에
실리지 않는다 — 드롭다운을 열어 고르되, 글자는 언어마다 다르니 `option[value]`
에서 읽어 온다.

#### 화면 변수는 워커마다 10 초 캐시된다

메뉴의 업그레이드 표시처럼 `Misc` 에서 오는 화면 변수는 `routes_static._cached_misc`
가 10 초 캐시한다. DB 를 직접 바꾼 검사는 그 수명을 넘겨 다시 연다.

#### 상태 어휘가 화면과 어긋나면 "없는 것" 이 된다

일정을 `APPROVED` 로 심었더니 저장은 됐는데 달력이 비었다. 달력이 보여 주는
상태는 따로 정해져 있다(`_CALENDAR_VISIBLE_STATES`: DRAFT·PENDING·RUNNING·
COMPLETED·FAILED). 픽스처를 만들 때는 **그 화면이 보여 주는 값**으로 넣어야
검사가 성립한다.

## 아직 없는 것

- **L3 C3~C5** — 시퀀스 활성화의 데몬 반영, 타이머의 재시작 뒤 상태 유지, 함수
  비활성화 때 출력이 확실히 꺼지는지(`.local/plans/2609_e2e_test_plan.md`).

## 실패했을 때

`.local/e2e-artifacts/<경로>.png` 에 그 순간의 전체 화면이, `.txt` 에 콘솔
오류와 실패 응답이 남는다. CI 는 이것을 아티팩트로 올린다.

## 규칙

- **실패한 E2E 는 머지를 막는다.** 막지 않으면 곧 빨간 불이 상시가 되고
  아무도 안 본다.
- **불안정한 검사는 고치거나 지운다.** 재시도로 덮지 않는다 — 덮는 순간
  검사 자체가 "조용한 실패" 를 저지른다.
- 현장 사고가 나면 **재현 시나리오를 L2 에 한 줄 더하는 것**을 수정의
  일부로 삼는다.
